{
  "capabilities_version": 163,
  "endpoints": {
    "DELETE /api/auth/api-keys/{apiKeyId}": {
      "auth": "required",
      "availability": "stable",
      "identity_scoped": true,
      "operation_id": "revokeApiKey",
      "required_tier": null
    },
    "GET /api/operations/schema": {
      "auth": "optional",
      "availability": "stable",
      "identity_scoped": false,
      "operation_id": "getOperationsSchema",
      "required_tier": null
    },
    "GET /api/uploads/multipart/{uploadId}/status": {
      "auth": "required",
      "availability": "stable",
      "identity_scoped": true,
      "operation_id": "multipartStatus",
      "required_tier": null
    },
    "GET /api/uploads/{id}/metadata": {
      "auth": "optional",
      "availability": "stable",
      "identity_scoped": true,
      "operation_id": "getUploadMetadata",
      "required_tier": null
    },
    "GET /api/v2/account/limits": {
      "auth": "required",
      "availability": "stable",
      "identity_scoped": true,
      "operation_id": "getAccountLimits",
      "required_tier": null
    },
    "GET /api/v2/credits/balance": {
      "auth": "required",
      "availability": "stable",
      "identity_scoped": true,
      "operation_id": "getCreditsBalance",
      "required_tier": null
    },
    "GET /api/v2/credits/usage": {
      "auth": "required",
      "availability": "stable",
      "identity_scoped": true,
      "operation_id": "getCreditsUsage",
      "required_tier": null
    },
    "GET /api/workflows": {
      "auth": "required",
      "availability": "stable",
      "identity_scoped": true,
      "operation_id": "listWorkflows",
      "required_tier": null
    },
    "GET /api/workflows/{id}/downloads": {
      "auth": "optional",
      "availability": "stable",
      "identity_scoped": true,
      "operation_id": "getWorkflowDownloads",
      "required_tier": null
    },
    "GET /api/workflows/{id}/events": {
      "auth": "optional",
      "availability": "stable",
      "identity_scoped": true,
      "operation_id": "streamWorkflowEvents",
      "required_tier": null
    },
    "GET /api/workflows/{id}/status": {
      "auth": "optional",
      "availability": "stable",
      "identity_scoped": true,
      "operation_id": "getWorkflowStatus",
      "required_tier": null
    },
    "GET /healthz": {
      "auth": "anonymous",
      "availability": "stable",
      "identity_scoped": false,
      "operation_id": "liveness",
      "required_tier": null
    },
    "GET /readyz": {
      "auth": "anonymous",
      "availability": "stable",
      "identity_scoped": false,
      "operation_id": "readiness",
      "required_tier": null
    },
    "PATCH /api/auth/profile": {
      "auth": "required",
      "availability": "stable",
      "identity_scoped": true,
      "operation_id": "updateProfile",
      "required_tier": null
    },
    "POST /api/audio-watermark/decode": {
      "auth": "required",
      "availability": "stable",
      "identity_scoped": false,
      "operation_id": "decodeAudioWatermark",
      "required_tier": null
    },
    "POST /api/auth/api-keys": {
      "auth": "required",
      "availability": "stable",
      "identity_scoped": true,
      "operation_id": "createApiKey",
      "required_tier": null
    },
    "POST /api/auth/change-password": {
      "auth": "required",
      "availability": "stable",
      "identity_scoped": true,
      "operation_id": "changePassword",
      "required_tier": null
    },
    "POST /api/auth/confirm-email-change": {
      "auth": "anonymous",
      "availability": "stable",
      "identity_scoped": false,
      "operation_id": "confirmEmailChange",
      "required_tier": null
    },
    "POST /api/auth/forgot-password": {
      "auth": "anonymous",
      "availability": "stable",
      "identity_scoped": false,
      "operation_id": "forgotPassword",
      "required_tier": null
    },
    "POST /api/auth/login": {
      "auth": "anonymous",
      "availability": "stable",
      "identity_scoped": false,
      "operation_id": "loginUser",
      "required_tier": null
    },
    "POST /api/auth/logout": {
      "auth": "required",
      "availability": "stable",
      "identity_scoped": true,
      "operation_id": "logoutUser",
      "required_tier": null
    },
    "POST /api/auth/register": {
      "auth": "anonymous",
      "availability": "stable",
      "identity_scoped": false,
      "operation_id": "registerUser",
      "required_tier": null
    },
    "POST /api/auth/reset-password": {
      "auth": "anonymous",
      "availability": "stable",
      "identity_scoped": false,
      "operation_id": "resetPassword",
      "required_tier": null
    },
    "POST /api/auth/verify-email": {
      "auth": "anonymous",
      "availability": "stable",
      "identity_scoped": false,
      "operation_id": "verifyEmail",
      "required_tier": null
    },
    "POST /api/billing/checkout": {
      "auth": "required",
      "availability": "stable",
      "identity_scoped": true,
      "operation_id": "createBillingCheckoutSession",
      "required_tier": null
    },
    "POST /api/contact": {
      "auth": "anonymous",
      "availability": "stable",
      "identity_scoped": false,
      "operation_id": "submitContact",
      "required_tier": null
    },
    "POST /api/external-imports": {
      "auth": "required",
      "availability": "stable",
      "identity_scoped": false,
      "operation_id": "createExternalImport",
      "required_tier": null
    },
    "POST /api/operations/{id}/retry": {
      "auth": "optional",
      "availability": "stable",
      "identity_scoped": true,
      "operation_id": "retryOperation",
      "required_tier": null
    },
    "POST /api/uploads": {
      "auth": "optional",
      "availability": "stable",
      "identity_scoped": false,
      "operation_id": "uploadFile",
      "required_tier": null
    },
    "POST /api/uploads/multipart/complete": {
      "auth": "optional",
      "availability": "stable",
      "identity_scoped": true,
      "operation_id": "multipartComplete",
      "required_tier": null
    },
    "POST /api/uploads/multipart/initiate": {
      "auth": "optional",
      "availability": "stable",
      "identity_scoped": false,
      "operation_id": "multipartInitiate",
      "required_tier": null
    },
    "POST /api/uploads/multipart/{uploadId}/keepalive": {
      "auth": "required",
      "availability": "stable",
      "identity_scoped": true,
      "operation_id": "multipartKeepalive",
      "required_tier": null
    },
    "POST /api/uploads/multipart/{uploadId}/presign": {
      "auth": "required",
      "availability": "stable",
      "identity_scoped": true,
      "operation_id": "multipartPresign",
      "required_tier": null
    },
    "POST /api/uploads/{id}/probe": {
      "auth": "required",
      "availability": "stable",
      "identity_scoped": true,
      "operation_id": "probeUpload",
      "required_tier": null
    },
    "POST /api/workflows": {
      "auth": "optional",
      "availability": "stable",
      "identity_scoped": true,
      "operation_id": "createWorkflow",
      "required_tier": null
    },
    "POST /api/workflows/{id}/cancel": {
      "auth": "required",
      "availability": "stable",
      "identity_scoped": true,
      "operation_id": "cancelWorkflow",
      "required_tier": null
    },
    "POST /api/workflows/{id}/resume": {
      "auth": "required",
      "availability": "stable",
      "identity_scoped": true,
      "operation_id": "resumeWorkflow",
      "required_tier": null
    }
  },
  "environment": "baseline",
  "generated_at": null,
  "operations": {
    "archive": {
      "accepts_mixed_types": true,
      "default": false,
      "description": "Bundle multiple files into a single archive",
      "input_model": "multi",
      "max_inputs": 50,
      "min_inputs": 2,
      "options": {
        "folder_structure": {
          "default": "flat",
          "description": "flat = all files together at the top level (default); by_job = a separate subfolder for each source (planned \u2014 not yet supported)",
          "per_value_availability": {
            "by_job": {
              "availability": "planned"
            }
          },
          "type": "enum",
          "values": [
            "flat",
            "by_job"
          ]
        },
        "format": {
          "default": "zip",
          "description": "Archive format",
          "type": "enum",
          "values": [
            "zip",
            "tar.gz"
          ]
        }
      },
      "sole_op": true
    },
    "audio_overlay": {
      "availability": "planned",
      "default": false,
      "description": "Mix a secondary audio asset (overlay) over a primary audio or\nvideo base. Use cases: DJ tags, podcast intros/outros, station\nIDs, jingles, branded mid-rolls.\n\n**Industry naming.** This is \"audio overlay\" or \"audio branding\"\n\u2014 NOT \"audio watermark\". Audio watermarking is steganographic\n(imperceptible identifier embedded in audio for ownership /\nforensic tracking) and is owned by the separate `audio_watermark`\noperation (planned per ticket I20).\n\nMulti-input (Path B with `role: base` + `role: overlay` per\nthe input role). `min_inputs: 2 = max_inputs: 2` for V2.0;\nmulti-overlay support is advertised as a planned feature\n(`features.multi_overlay_stack`) but Backend implementation\ndeferred.\n\nEach input is imported externally,\nreferenced from a vault connection, or an upstream `job_output`\n(uploads are NOT referenced directly inside inputs[]). An uploaded\noverlay or base enters via a `passthrough` source job referenced by\n`{ type: job_output, from: <id> }` (per ticket 4som89Uh). The base\nis whatever single-asset source the workflow\nupstream has \u2014 for `mime_groups.audio`, the base is the primary\naudio track; for `mime_groups.video`, the overlay is mixed into\nthe video's audio track and the visual track passes through\nuntouched.\n\n`planned` \u2014 not yet supported.\n",
      "features": {
        "multi_overlay_stack": {
          "availability": "planned",
          "description": "Allow up to 8 overlay inputs per job (currently capped at 1\noverlay). Per-overlay placement via\nper-input placement options. Backend support not yet\nconfirmed; tagged `planned`. Mirrors the same-named feature on\n`image_watermark`.\n"
        },
        "sync_to_silence": {
          "availability": "planned",
          "description": "Anchor overlay placement to detected silence regions in the\nbase track (e.g. drop a podcast jingle into the natural\nbreath after the host says \"back in a sec\"). Server-side\nsilence detection runs at workflow-create time; the actual\noverlay start position is reported on\n`OperationMetrics.overlay_anchor_ms`. Gates two extra\noptions: `silence_threshold_db` and `min_silence_ms`. backend\nsupport not yet shipped.\n"
        }
      },
      "input_model": "multi",
      "max_inputs": 2,
      "mime_groups": {
        "audio": {
          "availability": "planned",
          "mimes": [
            "audio/mpeg",
            "audio/aac",
            "audio/wav",
            "audio/ogg",
            "audio/flac"
          ],
          "options": {
            "duck_attack_ms": {
              "default": 50,
              "depends_on": {
                "mode": "duck"
              },
              "description": "Time (milliseconds) for the ducker to reach full\nattenuation once the overlay crosses `duck_threshold`.\nOnly applies when `mode: duck`.\n",
              "max": 5000,
              "min": 0,
              "type": "integer"
            },
            "duck_ratio": {
              "default": 4.0,
              "depends_on": {
                "mode": "duck"
              },
              "description": "How strongly the base ducks \u2014 higher values dip it more\naggressively. Only applies when `mode: duck`.\n",
              "max": 20.0,
              "min": 1.0,
              "type": "float"
            },
            "duck_release_ms": {
              "default": 300,
              "depends_on": {
                "mode": "duck"
              },
              "description": "Time (milliseconds) for the ducker to release back to\nunity gain after the overlay drops below\n`duck_threshold`. Only applies when `mode: duck`.\n",
              "max": 5000,
              "min": 0,
              "type": "integer"
            },
            "duck_threshold": {
              "default": -20.0,
              "depends_on": {
                "mode": "duck"
              },
              "description": "Level (dB) the overlay must reach before the base audio\nducks. Only applies when `mode: duck`.\n",
              "max": 0.0,
              "min": -60.0,
              "type": "float"
            },
            "duration": {
              "description": "Maximum duration (seconds) of the overlay AS PLAYED \u2014 the\noverlay asset is trimmed to this length if longer. Omit\n(or set 0) to play the overlay's full natural length.\n**Disambiguation:** this is overlay-trim duration, NOT\noutput duration. The output duration always matches the\nbase track's duration.\n",
              "min": 0.0,
              "type": "float"
            },
            "fade_in_ms": {
              "default": 0,
              "description": "Linear fade-in applied to the overlay (milliseconds).\nDefault 0 = hard start.\n",
              "max": 10000,
              "min": 0,
              "type": "integer"
            },
            "fade_out_ms": {
              "default": 0,
              "description": "Linear fade-out applied to the overlay (milliseconds).\nDefault 0 = hard stop.\n",
              "max": 10000,
              "min": 0,
              "type": "integer"
            },
            "loop": {
              "default": false,
              "description": "When true, the overlay loops continuously across the\nduration of the base track (or up to the trimmed\n`duration`). Useful for ambient bed music / background\nstings.\n",
              "type": "boolean"
            },
            "loop_interval": {
              "default": 0.0,
              "depends_on": {
                "loop": true
              },
              "description": "Silence gap (seconds) inserted between overlay loops.\nDefault 0 = back-to-back. Only applies when `loop: true`.\n",
              "max": 600.0,
              "min": 0.0,
              "type": "float"
            },
            "min_silence_ms": {
              "availability": "planned",
              "default": 500,
              "description": "Minimum silence duration (milliseconds) to qualify as an\nanchor region for `sync_to_silence`. Inert until that\nfeature flips to stable.\n",
              "max": 30000,
              "min": 50,
              "type": "integer"
            },
            "mode": {
              "default": "mix",
              "description": "How the overlay combines with the base audio:\n- `mix`: both play together (overlay added on top; both audible).\n- `duck`: the base audio dips while the overlay plays, then\n  returns \u2014 the broadcast voice-over pattern.\n- `replace`: mute the base for the overlay's duration\n  (overlay only; useful for hard-cut station IDs).\n",
              "per_value_availability": {
                "duck": {
                  "availability": "planned"
                }
              },
              "type": "enum",
              "values": [
                "mix",
                "duck",
                "replace"
              ]
            },
            "overlay_gain_db": {
              "default": -3.0,
              "description": "Gain applied to the overlay in decibels before mixing.\nDefault -3 dB matches typical podcast intro / DJ-tag\nlevel. Positive values amplify (clip risk); negative\nvalues attenuate. Clamped to range.\n",
              "max": 12.0,
              "min": -60.0,
              "type": "float"
            },
            "silence_threshold_db": {
              "availability": "planned",
              "default": -50.0,
              "description": "Silence threshold (dB) for the `sync_to_silence` feature.\nBase audio below this level for at least `min_silence_ms`\nqualifies as a silence region into which the overlay can\nbe anchored. Inert until\n`features.sync_to_silence.availability` flips to stable.\n",
              "max": -10.0,
              "min": -80.0,
              "type": "float"
            },
            "start_at": {
              "default": 0.0,
              "description": "Start position (seconds) on the base track at which the\noverlay begins playing. Default 0.0 = mix from the\nbeginning. Inert when\n`features.sync_to_silence.availability` flips to\n`stable` and `mode: sync_to_silence` is selected\n(placement is then anchored to detected silence).\n",
              "min": 0.0,
              "type": "float"
            }
          },
          "processing_class": {
            "long_form": {
              "availability": "planned",
              "constraints": {
                "max_input_duration": "PT12H",
                "max_input_size_bytes": 5000000000,
                "max_output_size_bytes": 5000000000
              },
              "per_tier_constraints": {
                "enterprise": {
                  "max_input_size_bytes": 120000000000,
                  "max_output_size_bytes": 120000000000
                },
                "max": {
                  "max_input_size_bytes": 50000000000,
                  "max_output_size_bytes": 50000000000
                }
              },
              "required_tier": "pro"
            },
            "short_form": {
              "availability": "planned",
              "constraints": {
                "max_input_duration": "PT5M",
                "max_input_size_bytes": 524288000,
                "max_output_size_bytes": 524288000
              }
            }
          }
        },
        "video": {
          "availability": "planned",
          "mimes": [
            "video/mp4",
            "video/webm"
          ],
          "options": {
            "duck_attack_ms": {
              "default": 50,
              "depends_on": {
                "mode": "duck"
              },
              "description": "Ducker attack (milliseconds). Only applies when `mode: duck`.",
              "max": 5000,
              "min": 0,
              "type": "integer"
            },
            "duck_ratio": {
              "default": 4.0,
              "depends_on": {
                "mode": "duck"
              },
              "description": "How strongly the base ducks \u2014 higher values dip it more aggressively. Only applies when `mode: duck`.",
              "max": 20.0,
              "min": 1.0,
              "type": "float"
            },
            "duck_release_ms": {
              "default": 300,
              "depends_on": {
                "mode": "duck"
              },
              "description": "Ducker release (milliseconds). Only applies when `mode: duck`.",
              "max": 5000,
              "min": 0,
              "type": "integer"
            },
            "duck_threshold": {
              "default": -20.0,
              "depends_on": {
                "mode": "duck"
              },
              "description": "Level (dB) the overlay must reach before the base audio ducks. Only applies when `mode: duck`.",
              "max": 0.0,
              "min": -60.0,
              "type": "float"
            },
            "duration": {
              "description": "Maximum duration (seconds) of the overlay AS PLAYED. The\noverlay asset is trimmed to this length if longer. Output\nvideo duration always matches the base.\n",
              "min": 0.0,
              "type": "float"
            },
            "fade_in_ms": {
              "default": 0,
              "description": "Linear fade-in applied to the overlay (milliseconds).",
              "max": 10000,
              "min": 0,
              "type": "integer"
            },
            "fade_out_ms": {
              "default": 0,
              "description": "Linear fade-out applied to the overlay (milliseconds).",
              "max": 10000,
              "min": 0,
              "type": "integer"
            },
            "loop": {
              "default": false,
              "description": "When true, the overlay loops continuously across the\nduration of the base video.\n",
              "type": "boolean"
            },
            "loop_interval": {
              "default": 0.0,
              "depends_on": {
                "loop": true
              },
              "description": "Silence gap (seconds) between overlay loops.",
              "max": 600.0,
              "min": 0.0,
              "type": "float"
            },
            "min_silence_ms": {
              "availability": "planned",
              "default": 500,
              "description": "Minimum silence duration (milliseconds) for\n`sync_to_silence`. Inert until that feature flips to\nstable.\n",
              "max": 30000,
              "min": 50,
              "type": "integer"
            },
            "mode": {
              "default": "mix",
              "description": "Same modes as for audio overlays. `replace` mutes the\nbase video's audio for the overlay's duration.\n",
              "per_value_availability": {
                "duck": {
                  "availability": "planned"
                }
              },
              "type": "enum",
              "values": [
                "mix",
                "duck",
                "replace"
              ]
            },
            "no_audio_track_behaviour": {
              "default": "reject",
              "description": "Controls behaviour when the base video has no audio\ntrack:\n- `reject`: workflow-create returns 422 (default \u2014 surfaces\n  the problem at the API edge rather than producing a\n  surprising silent-base output).\n- `create_silent`: synthesise a silent base track at the\n  video's framerate and mix the overlay into that.\n- `replace`: same as `create_silent` but signals intent\n  (\"the overlay IS the audio track\"), letting backend\n  skip the base-track synthesis when the overlay covers\n  the full video duration.\n",
              "type": "enum",
              "values": [
                "reject",
                "create_silent",
                "replace"
              ]
            },
            "overlay_gain_db": {
              "default": -3.0,
              "description": "Gain applied to the overlay in decibels before mixing.",
              "max": 12.0,
              "min": -60.0,
              "type": "float"
            },
            "silence_threshold_db": {
              "availability": "planned",
              "default": -50.0,
              "description": "Silence threshold (dB) for `sync_to_silence`. Inert\nuntil that feature flips to stable.\n",
              "max": -10.0,
              "min": -80.0,
              "type": "float"
            },
            "start_at": {
              "default": 0.0,
              "description": "Start position (seconds) on the base video's audio track\nat which the overlay begins playing.\n",
              "min": 0.0,
              "type": "float"
            }
          },
          "processing_class": {
            "long_form": {
              "availability": "planned",
              "constraints": {
                "max_input_duration": "PT12H",
                "max_input_size_bytes": 5000000000,
                "max_output_size_bytes": 5000000000
              },
              "per_tier_constraints": {
                "enterprise": {
                  "max_input_size_bytes": 120000000000,
                  "max_output_size_bytes": 120000000000
                },
                "max": {
                  "max_input_size_bytes": 50000000000,
                  "max_output_size_bytes": 50000000000
                }
              },
              "required_tier": "pro"
            },
            "short_form": {
              "availability": "planned",
              "constraints": {
                "max_input_duration": "PT5M",
                "max_input_size_bytes": 524288000,
                "max_output_size_bytes": 524288000
              }
            }
          }
        }
      },
      "min_inputs": 2,
      "per_role_cardinality": {
        "base": {
          "max": 1,
          "min": 1
        },
        "overlay": {
          "max": 1,
          "min": 1
        }
      },
      "sole_op": true
    },
    "audio_to_video": {
      "availability": "beta",
      "default": false,
      "description": "Produce a video from an audio input plus an OPTIONAL still image\noverlay. Use cases: podcast \u2192 YouTube uploads, audio-only tracks\nthat need a video container for platforms requiring it, branded\naudio releases with a static cover image.\n\n**Role asymmetry.** First role-based operation on the contract\nwith an OPTIONAL role \u2014 REQUIRED `base` (audio source) +\nOPTIONAL `overlay` (still image, 0..1). When `overlay` is\nomitted, the video uses a solid `background_color`. The\nmachine-readable cardinality lives in `per_role_cardinality`\non this schema.\n\n**Input vs output asymmetry.** The single `mime_groups.audio`\nblock keys the INPUT MIME family (the `base` role input is\naudio). The OUTPUT is always video \u2014 `output_format` controls\nthe container; `mime_groups` is NOT the output type. Mirrors\nthe `audio_overlay.video` group pattern (input-keyed).\n\nRe-encode required (audio \u2192 video composition always re-encodes).\nA lower framerate plus still-image tuning keeps file size tiny\nwhen the overlay is a still image (the common case).\n\nActivated to `availability: beta`: the backend is live (API\naccepts + publishes), so this is shape-stable + opt-in for callers\nand MUST NOT return `feature_not_available` \u2014 it may still change\nwith notice while beta.\n",
      "input_model": "multi",
      "max_inputs": 2,
      "mime_groups": {
        "audio": {
          "mimes": [
            "audio/mpeg",
            "audio/aac",
            "audio/wav",
            "audio/x-wav",
            "audio/flac",
            "audio/ogg",
            "audio/opus",
            "audio/mp4",
            "audio/x-m4a"
          ],
          "options": {
            "background_color": {
              "default": "#000000",
              "description": "Hex `#RRGGBB` solid background colour. Used only when\n`overlay` is omitted (no image input). When an `overlay`\nimage is provided, the background colour is invisible\nbehind the image (subject to `image_fit`).\n",
              "type": "string"
            },
            "framerate": {
              "default": 2,
              "description": "Output video framerate. Default `2` fps keeps the file size\ntiny for the common case of a single still overlay. Raise for\nanimated overlays (not supported in V1 \u2014 overlay must be\na still image) or platform requirements (e.g. YouTube\nrecommends 24+ fps; some platforms accept lower).\n",
              "max": 60,
              "min": 1,
              "type": "integer"
            },
            "image_fit": {
              "default": "contain",
              "description": "How to fit the overlay image into the output frame.\n`contain` = letterbox (preserve aspect ratio, may show\nbackground); `cover` = crop (fill the frame, may crop\nthe image); `stretch` = distort to fit (preserves no\naspect ratio). Default `contain` preserves the image\naesthetic.\n",
              "type": "enum",
              "values": [
                "contain",
                "cover",
                "stretch"
              ]
            },
            "output_format": {
              "default": "mp4",
              "description": "Output container format. V1 supports `mp4` only; webm and mov\nare planned additions.\n",
              "type": "enum",
              "values": [
                "mp4"
              ]
            },
            "output_resolution": {
              "default": "hd_720p",
              "description": "Output video resolution. `hd_720p` = 1280\u00d7720 (default),\n`fhd_1080p` = 1920\u00d71080, `sd_480p` = 854\u00d7480. Choose\nlower for small file size; higher when the overlay image\nquality warrants it.\n",
              "type": "enum",
              "values": [
                "hd_720p",
                "fhd_1080p",
                "sd_480p"
              ]
            }
          }
        }
      },
      "min_inputs": 1,
      "per_role_cardinality": {
        "base": {
          "max": 1,
          "min": 1
        },
        "overlay": {
          "max": 1,
          "min": 0
        }
      },
      "sole_op": true
    },
    "audio_watermark": {
      "availability": "planned",
      "default": false,
      "description": "Embed a steganographic forensic watermark into an audio asset (or\na video's audio track). The watermark is psychoacoustically masked\nto be inaudible and engineered to survive lossy compression,\ntranscoding, broadcast chains, and analog-hole capture (Cinavia /\nResemble PerTh territory).\n\n**Industry naming.** This is *audio watermarking* (steganographic;\nused for ownership, forensic tracking, content provenance) \u2014 NOT\n*audio overlay* (audible mixing of secondary audio for branding).\nAudio overlay is owned by the separate `audio_overlay` operation\n(per ticket I19).\n\nSingle-input. Watermark embedding takes the base asset and writes\nout an indistinguishable-to-the-ear copy with the encoded payload.\n\n**Decode endpoint.** A paired `POST /api/audio-watermark/decode`\nendpoint extracts the embedded watermark from a previously-marked\nasset, returning `{watermark_id, payload?, confidence, method,\ndetected_at}`. Decode is **own-watermarks-only** (rate-limited\n+ audited; the decoder will refuse to extract from media the\ncaller did not mark themselves) \u2014 pin per spike S11 acceptance.\n\n`planned` \u2014 not yet supported.\n",
      "input_model": "single",
      "mime_groups": {
        "audio": {
          "availability": "planned",
          "mimes": [
            "audio/mpeg",
            "audio/aac",
            "audio/wav",
            "audio/ogg",
            "audio/flac"
          ],
          "options": {
            "density": {
              "default": "normal",
              "description": "Watermark spatial density along the asset duration:\n- `sparse`: payload bits inserted at wider intervals;\n  decode confidence drops on short clips.\n- `normal` (default): balanced.\n- `dense`: maximum decode confidence on short clips at\n  the cost of slightly higher perceptual risk.\n",
              "type": "enum",
              "values": [
                "sparse",
                "normal",
                "dense"
              ]
            },
            "method": {
              "default": "auto",
              "description": "Watermarking algorithm:\n- `auto`: server selects the best available method for the\n  input (currently maps to `psychoacoustic`; will widen as\n  `neural` ships).\n- `psychoacoustic`: hides the mark below what the ear can\n  hear, so it stays inaudible. Survives MP3/AAC at moderate\n  bitrates and broadcast chains.\n- `neural`: machine-learning embedding. Higher survival\n  under aggressive re-encoding and microphone re-recording;\n  longer encoding time and non-deterministic.\n",
              "per_value_availability": {
                "neural": {
                  "availability": "planned"
                }
              },
              "type": "enum",
              "values": [
                "auto",
                "psychoacoustic",
                "neural"
              ]
            },
            "payload": {
              "description": "Optional caller-supplied payload to embed (max 256\ncharacters). Use cases: ownership marker, distribution\nchannel ID, license key. When omitted, the server\ngenerates an identifier automatically and embeds that\ninstead; the generated ID is returned on\n`OperationResult` so the caller can subsequently call\nthe decode endpoint to verify.\n",
              "required": false,
              "type": "string"
            },
            "robustness": {
              "default": "compression_resistant",
              "description": "Survival profile:\n- `light`: minimal payload-strength; smallest perceptual\n  footprint. Survives a single re-encode pass at moderate\n  bitrate.\n- `compression_resistant` (default): survives multiple\n  MP3/AAC re-encodes at typical streaming bitrates.\n- `broadcast_grade`: survives broadcast compression\n  chains, EQ, dynamic-range compression, and microphone\n  re-recording. Higher embedding time + slightly larger\n  perceptual footprint.\n",
              "type": "enum",
              "values": [
                "light",
                "compression_resistant",
                "broadcast_grade"
              ]
            }
          },
          "processing_class": {
            "long_form": {
              "availability": "planned",
              "constraints": {
                "max_input_duration": "PT12H",
                "max_input_size_bytes": 120000000000,
                "max_output_size_bytes": 120000000000
              },
              "required_tier": "enterprise"
            },
            "short_form": {
              "availability": "planned",
              "constraints": {
                "max_input_duration": "PT5M",
                "max_input_size_bytes": 524288000,
                "max_output_size_bytes": 524288000
              },
              "required_tier": "enterprise"
            }
          }
        },
        "video": {
          "availability": "planned",
          "mimes": [
            "video/mp4",
            "video/webm"
          ],
          "options": {
            "density": {
              "default": "normal",
              "description": "Watermark density along the asset duration.",
              "type": "enum",
              "values": [
                "sparse",
                "normal",
                "dense"
              ]
            },
            "method": {
              "default": "auto",
              "description": "Watermarking algorithm. Same as for an audio input;\nembedded into the video's audio track only.\n",
              "per_value_availability": {
                "neural": {
                  "availability": "planned"
                }
              },
              "type": "enum",
              "values": [
                "auto",
                "psychoacoustic",
                "neural"
              ]
            },
            "payload": {
              "description": "Optional caller-supplied payload to embed (max 256\ncharacters). When omitted, the server generates an\nidentifier automatically and embeds that instead.\n",
              "required": false,
              "type": "string"
            },
            "robustness": {
              "default": "compression_resistant",
              "description": "Survival profile. Same as for an audio input.",
              "type": "enum",
              "values": [
                "light",
                "compression_resistant",
                "broadcast_grade"
              ]
            }
          },
          "processing_class": {
            "long_form": {
              "availability": "planned",
              "constraints": {
                "max_input_duration": "PT12H",
                "max_input_size_bytes": 120000000000,
                "max_output_size_bytes": 120000000000
              },
              "required_tier": "enterprise"
            },
            "short_form": {
              "availability": "planned",
              "constraints": {
                "max_input_duration": "PT5M",
                "max_input_size_bytes": 524288000,
                "max_output_size_bytes": 524288000
              },
              "required_tier": "enterprise"
            }
          }
        }
      },
      "required_tier": "enterprise"
    },
    "compress": {
      "default": true,
      "description": "Reduce file size while maintaining acceptable quality",
      "input_model": "single",
      "mime_groups": {
        "audio": {
          "mimes": [
            "audio/mpeg",
            "audio/aac",
            "audio/ogg",
            "audio/flac",
            "audio/wav",
            "audio/mp4"
          ],
          "options": {
            "bitrate": {
              "default": 128,
              "description": "Output bitrate in kbps (CBR/ABR). Applies to lossy OUTPUTS. Only a caller-EXPLICIT bitrate against a lossless output is rejected as invalid_options (flac/wav have no bitrate target); the `default: 128` is never the basis for a rejection (a default a caller did not set is the server's to resolve, and is dropped for a lossless target). The output is the input format unless `output_format` re-targets it: with `output_format: original` the lossy/lossless distinction follows the input MIME (mpeg/aac/ogg/mp4 = lossy \u2192 bitrate applies; flac/wav input \u2192 an explicit bitrate is rejected); with an explicit `output_format`, it follows that target (flac/wav target \u2192 an explicit bitrate is rejected). Not expressed as `depends_on` \u2014 the lossy-output condition is an output-value \u00d7 input-MIME cross-axis the option-to-option `depends_on` cannot model without dropping `original` and breaking same-format callers. The other knobs (channels/sample_rate/normalize) apply to all formats.\n",
              "type": "enum",
              "value_type": "integer",
              "values": [
                64,
                96,
                128,
                192,
                256,
                320
              ]
            },
            "channels": {
              "description": "Number of audio channels (1 = mono, 2 = stereo)",
              "max": 2,
              "min": 1,
              "type": "integer"
            },
            "normalize": {
              "default": false,
              "description": "Apply loudness normalization (EBU R128 / -14 LUFS target)",
              "type": "boolean"
            },
            "output_format": {
              "default": "original",
              "description": "Output audio format. original = keep input format (default, same-format compression). Non-`original` values re-target the container/codec via the API \"compress+format\" facade (one canonicalized convert pass); see FORMAT.md.",
              "type": "enum",
              "values": [
                "original",
                "mp3",
                "aac",
                "ogg",
                "opus",
                "flac",
                "wav"
              ]
            },
            "sample_rate": {
              "description": "Audio sample rate in Hz",
              "type": "enum",
              "value_type": "integer",
              "values": [
                22050,
                44100,
                48000
              ]
            },
            "trim_end": {
              "description": "Trim from end in seconds (absent = to the end). Removes this many seconds from the end; with `trim_start` the kept clip is [`trim_start`, input_duration \u2212 `trim_end`]. See `trim_start`.\n",
              "min": 0,
              "type": "float"
            },
            "trim_start": {
              "description": "Trim from beginning in seconds (absent = from the start). The clip is cut before encoding, with the same from-end semantic as compress.video / convert / merge: the kept clip is [`trim_start`, input_duration \u2212 `trim_end`].\n",
              "min": 0,
              "type": "float"
            }
          }
        },
        "document_epub": {
          "mimes": [
            "application/epub+zip"
          ],
          "options": {
            "font_subsetting": {
              "availability": "planned",
              "default": true,
              "description": "Subset embedded fonts to only include glyphs used in the document. Significant size reduction for books with large font files. `planned` \u2014 not yet supported.",
              "type": "boolean"
            },
            "quality": {
              "default": 50,
              "description": "Compression quality (1 = smallest, 100 = best). The only document compression option supported today.",
              "max": 100,
              "min": 1,
              "type": "integer"
            },
            "strip_unused_css": {
              "availability": "planned",
              "default": false,
              "description": "Remove CSS rules not referenced by any content. Risk: may affect rendering in some readers. `planned` \u2014 not yet supported.",
              "type": "boolean"
            }
          }
        },
        "document_odf": {
          "mimes": [
            "application/vnd.oasis.opendocument.text",
            "application/vnd.oasis.opendocument.spreadsheet",
            "application/vnd.oasis.opendocument.presentation"
          ],
          "options": {
            "quality": {
              "default": 50,
              "description": "Compression quality (1 = smallest, 100 = best). The only document compression option supported today.",
              "max": 100,
              "min": 1,
              "type": "integer"
            },
            "strip_metadata": {
              "availability": "planned",
              "default": true,
              "description": "Remove document metadata (author, revision history, comments). `planned` \u2014 not yet supported.",
              "type": "boolean"
            },
            "strip_unused_styles": {
              "availability": "planned",
              "default": false,
              "description": "Remove style definitions not referenced in document content. `planned` \u2014 not yet supported.",
              "type": "boolean"
            }
          }
        },
        "document_office": {
          "mimes": [
            "application/vnd.openxmlformats-officedocument.wordprocessingml.document",
            "application/vnd.openxmlformats-officedocument.spreadsheetml.sheet",
            "application/vnd.openxmlformats-officedocument.presentationml.presentation"
          ],
          "options": {
            "quality": {
              "default": 50,
              "description": "Compression quality (1 = smallest, 100 = best). The only document compression option supported today.",
              "max": 100,
              "min": 1,
              "type": "integer"
            },
            "strip_hidden_data": {
              "availability": "planned",
              "default": true,
              "description": "Remove revisions, comments, personal info, hidden rows/columns, and speaker notes. `planned` \u2014 not yet supported.",
              "type": "boolean"
            },
            "strip_macros": {
              "availability": "planned",
              "default": true,
              "description": "Remove VBA macros and ActiveX controls. `planned` \u2014 not yet supported.",
              "type": "boolean"
            },
            "strip_unused_fonts": {
              "availability": "planned",
              "default": false,
              "description": "Remove embedded fonts not referenced in the document. Risk: may affect rendering on systems without the font installed. `planned` \u2014 not yet supported.",
              "type": "boolean"
            }
          }
        },
        "document_pdf": {
          "mimes": [
            "application/pdf"
          ],
          "options": {
            "colorspace": {
              "availability": "planned",
              "default": "unchanged",
              "description": "Output color space. `planned` \u2014 not yet supported (use `grayscale` for grayscale conversion).",
              "type": "enum",
              "values": [
                "unchanged",
                "rgb",
                "cmyk",
                "grayscale"
              ]
            },
            "flatten_forms": {
              "availability": "planned",
              "default": false,
              "description": "Flatten interactive form fields into static content. `planned` \u2014 not yet supported.",
              "type": "boolean"
            },
            "grayscale": {
              "default": false,
              "description": "Convert all images to grayscale (smaller output; discards colour).",
              "type": "boolean"
            },
            "image_dpi": {
              "description": "Downsample images above this DPI (72\u2013600 inclusive). Omit to use automatic DPI analysis. Out-of-range is rejected.",
              "max": 600,
              "min": 72,
              "type": "integer"
            },
            "pages": {
              "availability": "planned",
              "description": "Page selection (e.g. '1-5,8,10-12'). Omit to keep all pages. `planned` \u2014 not yet supported.",
              "type": "string"
            },
            "profile": {
              "description": "Compression preset, pinned directly (overrides the quality\u2192preset mapping). screen = lowest-res / smallest, ebook = mid, printer = 300dpi, prepress = print-production. Optional \u2014 omit to let `quality` drive the preset.",
              "type": "enum",
              "values": [
                "screen",
                "ebook",
                "printer",
                "prepress"
              ]
            },
            "quality": {
              "default": 50,
              "description": "Compression quality (1 = smallest, 100 = best). Maps to a compression preset. Overridden by `profile` when that is set.",
              "max": 100,
              "min": 1,
              "type": "integer"
            }
          }
        },
        "image": {
          "max_output_pixels": 16000000,
          "mimes": [
            "image/gif",
            "image/tiff"
          ],
          "options": {
            "auto_orient": {
              "default": true,
              "description": "Bake EXIF orientation into the pixels (honour the camera/device rotation tag) so the output displays upright everywhere, then drop the now-redundant orientation tag. Default `true`; no-op for inputs without an orientation tag. On an already-rotated image the pixels are re-encoded (near-lossless), which can drop other (non-orientation) metadata even with `metadata: keep`.",
              "honored_on": [
                "same_format",
                "format_change"
              ],
              "type": "boolean"
            },
            "color_profile": {
              "default": "keep",
              "description": "Colour-profile handling \u2014 a dedicated control, separate from `metadata` so that stripping metadata never silently shifts colours. `keep` (default) = preserve your colours: embed the ICC profile where the output format can carry one, otherwise convert to sRGB and bake the colours in so they survive (never a silent drop \u2014 for TIFF inputs the gamut is baked to sRGB; GIF carries no profile so this is a no-op); `srgb` = convert to sRGB (transform pixels to sRGB, emit untagged \u2014 no embedded ICC, the web default; CMYK/grayscale best-effort); `strip` = drop the profile (smallest \u2014 only when the consumer manages colour itself). On compress `srgb` is `planned` for this group (the sRGB pixel transform is wired for JPEG/PNG only so far); `keep` and `strip` are live.",
              "honored_on": [
                "same_format",
                "format_change"
              ],
              "per_value_availability": {
                "srgb": {
                  "availability": "planned"
                }
              },
              "type": "enum",
              "values": [
                "keep",
                "srgb",
                "strip"
              ]
            },
            "fit": {
              "default": "max",
              "depends_on": {
                "height": "set",
                "logic": "or",
                "width": "set"
              },
              "description": "Resize mode. `max` (default) = fit within the given bounds, preserving aspect ratio and never upscaling (caps at the source dimensions \u2014 this is the \"no-enlarge\" behaviour). `crop`/`scale` = exact dimensions (`scale` deliberately enlarges past the source). Applies only when width or height is set.",
              "honored_on": [
                "same_format",
                "format_change"
              ],
              "type": "enum",
              "values": [
                "max",
                "crop",
                "scale"
              ]
            },
            "height": {
              "description": "Target height in pixels (1-16384; width \u00d7 height within the group's max output area). Resizes the image (one output). Optional \u2014 see `width`.",
              "honored_on": [
                "same_format",
                "format_change"
              ],
              "max": 16384,
              "min": 1,
              "type": "integer"
            },
            "metadata": {
              "default": "strip",
              "description": "Metadata handling. `strip` = remove all EXIF/IPTC/XMP (default). `keep` = preserve EXIF, colour profile, and XMP. `all` is a DEPRECATED alias of `strip` (kept for older callers; emits Deprecation/Sunset headers \u2014 migrate to `strip`). Any other value is rejected.",
              "honored_on": [
                "same_format"
              ],
              "per_value_availability": {
                "all": {
                  "availability": "deprecated"
                }
              },
              "type": "enum",
              "values": [
                "strip",
                "keep",
                "all"
              ]
            },
            "output_format": {
              "default": "original",
              "description": "Output format. original = keep input format (default); webp = recompress to WebP. Concrete format changes (jpeg/png/avif/gif/tiff) are handled by the convert operation. `auto` / `smallest` are PLANNED auto-format directives \u2014 the server encodes to several candidate formats and returns the best (`auto` = the most suitable; `smallest` = the absolute fewest bytes), reporting the chosen format via the result `mime_type`. They are selection strategies, not concrete targets, and stay `planned` until supported. See FORMAT.md.",
              "per_value_availability": {
                "auto": {
                  "availability": "planned"
                },
                "smallest": {
                  "availability": "planned"
                }
              },
              "type": "enum",
              "values": [
                "original",
                "webp",
                "auto",
                "smallest"
              ]
            },
            "quality": {
              "default": 80,
              "description": "Compression quality (1 = smallest file, 100 = best quality). For animated GIF this is honoured but real-world size reduction is typically small.",
              "honored_on": [
                "same_format"
              ],
              "max": 100,
              "min": 1,
              "type": "integer"
            },
            "width": {
              "description": "Target width in pixels (1-16384; width \u00d7 height must not exceed the group's maximum output area). Resizes the image as part of the operation (one transformed output, replacing the source). Optional; omit to keep the source dimensions.",
              "honored_on": [
                "same_format",
                "format_change"
              ],
              "max": 16384,
              "min": 1,
              "type": "integer"
            }
          }
        },
        "image_avif": {
          "max_output_pixels": 16000000,
          "mimes": [
            "image/avif"
          ],
          "options": {
            "auto_orient": {
              "default": true,
              "description": "Bake EXIF orientation into the pixels (honour the camera/device rotation tag) so the output displays upright everywhere, then drop the now-redundant orientation tag. Default `true`; no-op for inputs without an orientation tag. On an already-rotated image the pixels are re-encoded (near-lossless), which can drop other (non-orientation) metadata even with `metadata: keep`.",
              "honored_on": [
                "same_format",
                "format_change"
              ],
              "type": "boolean"
            },
            "avif_speed": {
              "default": 4,
              "description": "AVIF encode speed (1-10). Lower = smaller file, slower encode; higher = faster, larger. Default 4.",
              "honored_on": [
                "same_format"
              ],
              "max": 10,
              "min": 1,
              "type": "integer"
            },
            "color_profile": {
              "default": "keep",
              "description": "Colour-profile handling \u2014 a dedicated control, separate from `metadata` so that stripping metadata never silently shifts colours. `keep` (default) = preserve your colours: embed the ICC profile where the output format can carry one, otherwise convert to sRGB and bake the colours in so they survive (never a silent drop \u2014 for AVIF the gamut is baked to sRGB); `srgb` = convert to sRGB (transform pixels to sRGB, emit untagged \u2014 no embedded ICC, the web default; CMYK/grayscale best-effort); `strip` = drop the profile (smallest \u2014 only when the consumer manages colour itself). All three values are live on compress AVIF \u2014 `srgb` flipped planned->stable 2026-06-30 on the lambdas proof (wide-gamut-p3-icc.avif: srgb bakes untagged, whole-image RMSE 0.041 / 92% pixels shifted vs strip). Note: on AVIF `keep` and `srgb` both bake the gamut to sRGB by design, so keep_px == srgb_px; the proving inequality is srgb != strip, which holds.",
              "honored_on": [
                "same_format",
                "format_change"
              ],
              "type": "enum",
              "values": [
                "keep",
                "srgb",
                "strip"
              ]
            },
            "encoding_mode": {
              "default": "quality",
              "description": "Compression mode. `quality` = the `quality` slider drives the encode (default). `target_size` = automatically find the highest quality that fits a byte budget (set via `target_size_bytes`); if the budget can't be met even at the lowest quality, the closest result is returned, and the original is kept if re-encoding wouldn't be smaller. `auto_quality` = hit a named perceptual-quality target (set via `quality_preset`) and let the worker pick the encoder quality that meets it \u2014 the perceptual twin of `target_size` (which targets bytes); the result reports the chosen quality. AVIF encoding is slower, so the search is bounded and may stop slightly above the budget.",
              "honored_on": [
                "same_format"
              ],
              "type": "enum",
              "values": [
                "quality",
                "target_size",
                "auto_quality"
              ]
            },
            "fit": {
              "default": "max",
              "depends_on": {
                "height": "set",
                "logic": "or",
                "width": "set"
              },
              "description": "Resize mode. `max` (default) = fit within the given bounds, preserving aspect ratio and never upscaling (caps at the source dimensions \u2014 this is the \"no-enlarge\" behaviour). `crop`/`scale` = exact dimensions (`scale` deliberately enlarges past the source). Applies only when width or height is set.",
              "honored_on": [
                "same_format",
                "format_change"
              ],
              "type": "enum",
              "values": [
                "max",
                "crop",
                "scale"
              ]
            },
            "height": {
              "description": "Target height in pixels (1-16384; width \u00d7 height within the group's max output area). Resizes the image (one output). Optional \u2014 see `width`.",
              "honored_on": [
                "same_format",
                "format_change"
              ],
              "max": 16384,
              "min": 1,
              "type": "integer"
            },
            "metadata": {
              "default": "strip",
              "description": "Metadata handling. `strip` = remove all EXIF/IPTC/XMP (default, the only supported behaviour). `all` is a DEPRECATED alias of `strip` (migrate to `strip`). `keep` is NOT offered for AVIF \u2014 it is re-encoded from pixels and cannot preserve metadata.",
              "honored_on": [
                "same_format"
              ],
              "per_value_availability": {
                "all": {
                  "availability": "deprecated"
                }
              },
              "type": "enum",
              "values": [
                "strip",
                "all"
              ]
            },
            "output_format": {
              "default": "original",
              "description": "Output format. original = keep input format (default); webp = recompress to WebP. Concrete format changes (jpeg/png/avif/gif/tiff) are handled by the convert operation. `auto` / `smallest` are PLANNED auto-format directives \u2014 the server encodes to several candidate formats and returns the best (`auto` = the most suitable; `smallest` = the absolute fewest bytes), reporting the chosen format via the result `mime_type`. They are selection strategies, not concrete targets, and stay `planned` until supported. See FORMAT.md.",
              "per_value_availability": {
                "auto": {
                  "availability": "planned"
                },
                "smallest": {
                  "availability": "planned"
                }
              },
              "type": "enum",
              "values": [
                "original",
                "webp",
                "auto",
                "smallest"
              ]
            },
            "quality": {
              "default": 80,
              "depends_on": {
                "encoding_mode": "quality"
              },
              "description": "Compression quality (1 = smallest file, 100 = best quality). Active in `quality` mode (default); in `target_size` mode the quality is chosen automatically to hit the size budget, so this option is inactive.",
              "honored_on": [
                "same_format"
              ],
              "max": 100,
              "min": 1,
              "type": "integer"
            },
            "quality_preset": {
              "default": "good",
              "depends_on": {
                "encoding_mode": "auto_quality"
              },
              "description": "Named perceptual-quality target for `encoding_mode: auto_quality` (best > good > fair > low; `good` is the default). The worker runs a perceptual-target search and picks the encoder quality that meets the named tier \u2014 the wire carries the NAME only, so the tier\u2192target mapping stays worker-internal and calibration-tunable without a contract change. Requires `encoding_mode: auto_quality`. The achieved perceptual score is reported on the result as `metrics.measured_quality` (+ `metrics.quality_metric`).",
              "honored_on": [
                "same_format"
              ],
              "type": "enum",
              "values": [
                "best",
                "good",
                "fair",
                "low"
              ]
            },
            "target_size_bytes": {
              "depends_on": {
                "encoding_mode": "target_size"
              },
              "description": "Target output size in bytes (min 1024 = 1 KiB). The encoder searches for the highest quality that lands at or under this size; if the target can't be met even at the lowest quality, the closest result is returned (the result reports the chosen quality and whether the target was met). Requires `encoding_mode: target_size`.",
              "honored_on": [
                "same_format"
              ],
              "min": 1024,
              "type": "integer"
            },
            "width": {
              "description": "Target width in pixels (1-16384; width \u00d7 height must not exceed the group's maximum output area). Resizes the image as part of the operation (one transformed output, replacing the source). Optional; omit to keep the source dimensions.",
              "honored_on": [
                "same_format",
                "format_change"
              ],
              "max": 16384,
              "min": 1,
              "type": "integer"
            }
          }
        },
        "image_jpeg": {
          "max_output_pixels": 16000000,
          "mimes": [
            "image/jpeg"
          ],
          "options": {
            "auto_orient": {
              "default": true,
              "description": "Bake EXIF orientation into the pixels (honour the camera/device rotation tag) so the output displays upright everywhere, then drop the now-redundant orientation tag. Default `true`; no-op for inputs without an orientation tag. On an already-rotated image the pixels are re-encoded (near-lossless), which can drop other (non-orientation) metadata even with `metadata: keep`.",
              "honored_on": [
                "same_format",
                "format_change"
              ],
              "type": "boolean"
            },
            "chroma_subsampling": {
              "description": "JPEG chroma subsampling \u2014 the colour-resolution/size trade-off. `420` (4:2:0) = smallest, chroma at quarter resolution (typical for web photos); `422` (4:2:2) = half horizontal resolution; `444` (4:4:4) = full chroma (largest, best for sharp colour edges / text). JPEG only. INCOMPATIBLE with `lossless` (any chroma value): a lossless JPEG copies the existing coefficients and cannot resample chroma.",
              "honored_on": [
                "same_format"
              ],
              "type": "enum",
              "values": [
                "420",
                "422",
                "444"
              ]
            },
            "color_profile": {
              "default": "keep",
              "description": "Colour-profile handling \u2014 a dedicated control, separate from `metadata` so that stripping metadata never silently shifts colours. `keep` (default) = preserve your colours: embed the ICC profile where the output format can carry one, otherwise convert to sRGB and bake the colours in so they survive (never a silent drop); `srgb` = convert to sRGB (transform pixels to sRGB, emit untagged \u2014 no embedded ICC, the web default; CMYK/grayscale best-effort); `strip` = drop the profile (smallest \u2014 only when the consumer manages colour itself).",
              "honored_on": [
                "same_format",
                "format_change"
              ],
              "type": "enum",
              "values": [
                "keep",
                "srgb",
                "strip"
              ]
            },
            "encoding_mode": {
              "default": "quality",
              "description": "Compression mode. `quality` = the `quality` slider drives the encode (default). `target_size` = automatically find the highest quality that fits a byte budget (set via `target_size_bytes`); if the budget can't be met even at the lowest quality, the closest result is returned, and the original is kept if re-encoding wouldn't be smaller. `auto_quality` = hit a named perceptual-quality target (set via `quality_preset`) and let the worker pick the encoder quality that meets it \u2014 the perceptual twin of `target_size` (which targets bytes); the result reports the chosen quality.",
              "honored_on": [
                "same_format"
              ],
              "type": "enum",
              "values": [
                "quality",
                "target_size",
                "auto_quality"
              ]
            },
            "fit": {
              "default": "max",
              "depends_on": {
                "height": "set",
                "logic": "or",
                "width": "set"
              },
              "description": "Resize mode. `max` (default) = fit within the given bounds, preserving aspect ratio and never upscaling (caps at the source dimensions \u2014 this is the \"no-enlarge\" behaviour). `crop`/`scale` = exact dimensions (`scale` deliberately enlarges past the source). Applies only when width or height is set.",
              "honored_on": [
                "same_format",
                "format_change"
              ],
              "type": "enum",
              "values": [
                "max",
                "crop",
                "scale"
              ]
            },
            "height": {
              "description": "Target height in pixels (1-16384; width \u00d7 height within the group's max output area). Resizes the image (one output). Optional \u2014 see `width`.",
              "honored_on": [
                "same_format",
                "format_change"
              ],
              "max": 16384,
              "min": 1,
              "type": "integer"
            },
            "lossless": {
              "default": false,
              "depends_on": {
                "encoding_mode": "quality"
              },
              "description": "Lossless JPEG re-optimisation: re-packs the file WITHOUT re-encoding the pixels \u2014 no quality loss (it cannot recover detail a lossy JPEG already lost). Only in `quality` mode \u2014 incompatible with `target_size` (hitting a byte budget is inherently lossy).",
              "honored_on": [
                "same_format"
              ],
              "type": "boolean"
            },
            "metadata": {
              "default": "strip",
              "description": "Metadata handling. `strip` = remove all EXIF/IPTC/XMP (default). `keep` = preserve EXIF, colour profile, and XMP. `all` is a DEPRECATED alias of `strip` (kept for older callers; emits Deprecation/Sunset headers \u2014 migrate to `strip`). Any other value is rejected.",
              "honored_on": [
                "same_format"
              ],
              "per_value_availability": {
                "all": {
                  "availability": "deprecated"
                }
              },
              "type": "enum",
              "values": [
                "strip",
                "keep",
                "all"
              ]
            },
            "output_format": {
              "default": "original",
              "description": "Output format. original = keep input format (default); webp = recompress to WebP. Concrete format changes (jpeg/png/avif/gif/tiff) are handled by the convert operation. `auto` / `smallest` are PLANNED auto-format directives \u2014 the server encodes to several candidate formats and returns the best (`auto` = the most suitable; `smallest` = the absolute fewest bytes), reporting the chosen format via the result `mime_type`. They are selection strategies, not concrete targets, and stay `planned` until supported. See FORMAT.md.",
              "per_value_availability": {
                "auto": {
                  "availability": "planned"
                },
                "smallest": {
                  "availability": "planned"
                }
              },
              "type": "enum",
              "values": [
                "original",
                "webp",
                "auto",
                "smallest"
              ]
            },
            "progressive": {
              "default": true,
              "description": "Enable progressive JPEG rendering (a low-quality preview renders first, then refines). JPEG only.",
              "honored_on": [
                "same_format"
              ],
              "type": "boolean"
            },
            "quality": {
              "default": 80,
              "depends_on": {
                "encoding_mode": "quality"
              },
              "description": "Compression quality (1 = smallest file, 100 = best quality). Active in `quality` mode (default); in `target_size` mode the quality is chosen automatically to hit the size budget, so this option is inactive.",
              "honored_on": [
                "same_format"
              ],
              "max": 100,
              "min": 1,
              "type": "integer"
            },
            "quality_preset": {
              "default": "good",
              "depends_on": {
                "encoding_mode": "auto_quality"
              },
              "description": "Named perceptual-quality target for `encoding_mode: auto_quality` (best > good > fair > low; `good` is the default). The worker runs a perceptual-target search and picks the encoder quality that meets the named tier \u2014 the wire carries the NAME only, so the tier\u2192target mapping stays worker-internal and calibration-tunable without a contract change. Requires `encoding_mode: auto_quality`. The achieved perceptual score is reported on the result as `metrics.measured_quality` (+ `metrics.quality_metric`).",
              "honored_on": [
                "same_format"
              ],
              "type": "enum",
              "values": [
                "best",
                "good",
                "fair",
                "low"
              ]
            },
            "target_size_bytes": {
              "depends_on": {
                "encoding_mode": "target_size"
              },
              "description": "Target output size in bytes (min 1024 = 1 KiB). The encoder searches for the highest quality that lands at or under this size; if the target can't be met even at the lowest quality, the closest result is returned (the result reports the chosen quality and whether the target was met). Requires `encoding_mode: target_size`.",
              "honored_on": [
                "same_format"
              ],
              "min": 1024,
              "type": "integer"
            },
            "width": {
              "description": "Target width in pixels (1-16384; width \u00d7 height must not exceed the group's maximum output area). Resizes the image as part of the operation (one transformed output, replacing the source). Optional; omit to keep the source dimensions.",
              "honored_on": [
                "same_format",
                "format_change"
              ],
              "max": 16384,
              "min": 1,
              "type": "integer"
            }
          }
        },
        "image_png": {
          "max_output_pixels": 16000000,
          "mimes": [
            "image/png"
          ],
          "options": {
            "auto_orient": {
              "default": true,
              "description": "Bake EXIF orientation into the pixels (honour the camera/device rotation tag) so the output displays upright everywhere, then drop the now-redundant orientation tag. Default `true`; no-op for inputs without an orientation tag. On an already-rotated image the pixels are re-encoded (near-lossless), which can drop other (non-orientation) metadata even with `metadata: keep`.",
              "honored_on": [
                "same_format",
                "format_change"
              ],
              "type": "boolean"
            },
            "color_profile": {
              "default": "keep",
              "description": "Colour-profile handling \u2014 a dedicated control, separate from `metadata` so that stripping metadata never silently shifts colours. `keep` (default) = preserve your colours: embed the ICC profile where the output format can carry one, otherwise convert to sRGB and bake the colours in so they survive (never a silent drop); `srgb` = convert to sRGB (transform pixels to sRGB, emit untagged \u2014 no embedded ICC, the web default; CMYK/grayscale best-effort); `strip` = drop the profile (smallest \u2014 only when the consumer manages colour itself).",
              "honored_on": [
                "same_format",
                "format_change"
              ],
              "type": "enum",
              "values": [
                "keep",
                "srgb",
                "strip"
              ]
            },
            "fit": {
              "default": "max",
              "depends_on": {
                "height": "set",
                "logic": "or",
                "width": "set"
              },
              "description": "Resize mode. `max` (default) = fit within the given bounds, preserving aspect ratio and never upscaling (caps at the source dimensions \u2014 this is the \"no-enlarge\" behaviour). `crop`/`scale` = exact dimensions (`scale` deliberately enlarges past the source). Applies only when width or height is set.",
              "honored_on": [
                "same_format",
                "format_change"
              ],
              "type": "enum",
              "values": [
                "max",
                "crop",
                "scale"
              ]
            },
            "height": {
              "description": "Target height in pixels (1-16384; width \u00d7 height within the group's max output area). Resizes the image (one output). Optional \u2014 see `width`.",
              "honored_on": [
                "same_format",
                "format_change"
              ],
              "max": 16384,
              "min": 1,
              "type": "integer"
            },
            "metadata": {
              "default": "strip",
              "description": "Metadata handling. `strip` = remove all EXIF/IPTC/XMP (default). `keep` = preserve EXIF, colour profile, and XMP. `all` is a DEPRECATED alias of `strip` (kept for older callers; emits Deprecation/Sunset headers \u2014 migrate to `strip`). Any other value is rejected.",
              "honored_on": [
                "same_format"
              ],
              "per_value_availability": {
                "all": {
                  "availability": "deprecated"
                }
              },
              "type": "enum",
              "values": [
                "strip",
                "keep",
                "all"
              ]
            },
            "optimization_level": {
              "default": 3,
              "description": "PNG optimization effort (0-6). Higher = smaller file, slower encode. Default 3.",
              "honored_on": [
                "same_format"
              ],
              "max": 6,
              "min": 0,
              "type": "integer"
            },
            "output_format": {
              "default": "original",
              "description": "Output format. original = keep input format (default); webp = recompress to WebP. Concrete format changes (jpeg/png/avif/gif/tiff) are handled by the convert operation. `auto` / `smallest` are PLANNED auto-format directives \u2014 the server encodes to several candidate formats and returns the best (`auto` = the most suitable; `smallest` = the absolute fewest bytes), reporting the chosen format via the result `mime_type`. They are selection strategies, not concrete targets, and stay `planned` until supported. See FORMAT.md.",
              "per_value_availability": {
                "auto": {
                  "availability": "planned"
                },
                "smallest": {
                  "availability": "planned"
                }
              },
              "type": "enum",
              "values": [
                "original",
                "webp",
                "auto",
                "smallest"
              ]
            },
            "quality": {
              "default": 80,
              "description": "Compression quality (1-100). For PNG this is the SINGLE lossy/lossless control: `quality` < 100 applies lossy palette quantization (imagequant \u2014 licence-cleared for our server-side SaaS backend, `kkajDFXD`; the value is the quantization level), while `quality: 100` keeps the PNG lossless (oxipng optimisation only \u2014 tune the lossless effort via `optimization_level`). 1 = smallest file, 100 = best quality / lossless. There is no separate lossy/lossless toggle for PNG \u2014 it is purely quality-driven.",
              "honored_on": [
                "same_format"
              ],
              "max": 100,
              "min": 1,
              "type": "integer"
            },
            "width": {
              "description": "Target width in pixels (1-16384; width \u00d7 height must not exceed the group's maximum output area). Resizes the image as part of the operation (one transformed output, replacing the source). Optional; omit to keep the source dimensions.",
              "honored_on": [
                "same_format",
                "format_change"
              ],
              "max": 16384,
              "min": 1,
              "type": "integer"
            }
          }
        },
        "image_svg": {
          "mimes": [
            "image/svg+xml"
          ],
          "options": {
            "metadata": {
              "default": "strip",
              "description": "Metadata handling. `strip` = remove all metadata (default, the only supported behaviour). `all` is a DEPRECATED alias of `strip` (migrate to `strip`). `keep` is NOT offered for SVG \u2014 optimising the markup cannot preserve a metadata block.",
              "honored_on": [
                "same_format"
              ],
              "per_value_availability": {
                "all": {
                  "availability": "deprecated"
                }
              },
              "type": "enum",
              "values": [
                "strip",
                "all"
              ]
            },
            "output_format": {
              "default": "original",
              "description": "Output format. original = keep input format (default); webp = recompress to WebP. Concrete format changes are handled by the convert operation. `auto` / `smallest` are PLANNED auto-format directives (the server picks the best/smallest candidate format, reported via the result `mime_type`) \u2014 selection strategies, not concrete targets, planned until supported. See FORMAT.md.",
              "per_value_availability": {
                "auto": {
                  "availability": "planned"
                },
                "smallest": {
                  "availability": "planned"
                }
              },
              "type": "enum",
              "values": [
                "original",
                "webp",
                "auto",
                "smallest"
              ]
            }
          }
        },
        "image_webp": {
          "max_output_pixels": 16000000,
          "mimes": [
            "image/webp"
          ],
          "options": {
            "auto_orient": {
              "default": true,
              "description": "Bake EXIF orientation into the pixels (honour the camera/device rotation tag) so the output displays upright everywhere, then drop the now-redundant orientation tag. Default `true`; no-op for inputs without an orientation tag. On an already-rotated image the pixels are re-encoded (near-lossless), which can drop other (non-orientation) metadata even with `metadata: keep`.",
              "honored_on": [
                "same_format",
                "format_change"
              ],
              "type": "boolean"
            },
            "color_profile": {
              "default": "keep",
              "description": "Colour-profile handling \u2014 a dedicated control, separate from `metadata` so that stripping metadata never silently shifts colours. `keep` (default) = preserve your colours: embed the ICC profile where the output format can carry one, otherwise convert to sRGB and bake the colours in so they survive (never a silent drop); `srgb` = convert to sRGB (transform pixels to sRGB, emit untagged \u2014 no embedded ICC, the web default; CMYK/grayscale best-effort); `strip` = drop the profile (smallest \u2014 only when the consumer manages colour itself). All three values are live on the WebP same-format route \u2014 the sRGB pixel transform is staging-proven (lambdas compression-image git-13b5000: srgb output untagged + a real pixel shift vs strip).",
              "honored_on": [
                "same_format",
                "format_change"
              ],
              "type": "enum",
              "values": [
                "keep",
                "srgb",
                "strip"
              ]
            },
            "encoding_mode": {
              "default": "quality",
              "description": "Compression mode. `quality` = the `quality` slider drives the encode (default). `target_size` = automatically find the highest quality that fits a byte budget (set via `target_size_bytes`); if the budget can't be met even at the lowest quality, the closest result is returned, and the original is kept if re-encoding wouldn't be smaller. `auto_quality` = hit a named perceptual-quality target (set via `quality_preset`) and let the worker pick the encoder quality that meets it \u2014 the perceptual twin of `target_size` (which targets bytes); the result reports the chosen quality.",
              "honored_on": [
                "same_format"
              ],
              "type": "enum",
              "values": [
                "quality",
                "target_size",
                "auto_quality"
              ]
            },
            "fit": {
              "default": "max",
              "depends_on": {
                "height": "set",
                "logic": "or",
                "width": "set"
              },
              "description": "Resize mode. `max` (default) = fit within the given bounds, preserving aspect ratio and never upscaling (caps at the source dimensions \u2014 this is the \"no-enlarge\" behaviour). `crop`/`scale` = exact dimensions (`scale` deliberately enlarges past the source). Applies only when width or height is set.",
              "honored_on": [
                "same_format",
                "format_change"
              ],
              "type": "enum",
              "values": [
                "max",
                "crop",
                "scale"
              ]
            },
            "height": {
              "description": "Target height in pixels (1-16384; width \u00d7 height within the group's max output area). Resizes the image (one output). Optional \u2014 see `width`.",
              "honored_on": [
                "same_format",
                "format_change"
              ],
              "max": 16384,
              "min": 1,
              "type": "integer"
            },
            "lossless": {
              "default": false,
              "depends_on": {
                "encoding_mode": "quality"
              },
              "description": "Encode WebP losslessly: a genuine lossless WebP encode. Note a lossless encode can be LARGER than a lossy one at the same visual quality. Only in `quality` mode \u2014 incompatible with `target_size` (hitting a byte budget is inherently lossy).",
              "honored_on": [
                "same_format"
              ],
              "type": "boolean"
            },
            "metadata": {
              "default": "strip",
              "description": "Metadata handling. `strip` = remove all EXIF/IPTC/XMP (default). `keep` = preserve EXIF, colour profile, and XMP. `all` is a DEPRECATED alias of `strip` (kept for older callers; emits Deprecation/Sunset headers \u2014 migrate to `strip`). Any other value is rejected.",
              "honored_on": [
                "same_format"
              ],
              "per_value_availability": {
                "all": {
                  "availability": "deprecated"
                }
              },
              "type": "enum",
              "values": [
                "strip",
                "keep",
                "all"
              ]
            },
            "output_format": {
              "default": "original",
              "description": "Output format. original = keep input format (default); webp = recompress to WebP. Concrete format changes (jpeg/png/avif/gif/tiff) are handled by the convert operation. `auto` / `smallest` are PLANNED auto-format directives \u2014 the server encodes to several candidate formats and returns the best (`auto` = the most suitable; `smallest` = the absolute fewest bytes), reporting the chosen format via the result `mime_type`. They are selection strategies, not concrete targets, and stay `planned` until supported. See FORMAT.md.",
              "per_value_availability": {
                "auto": {
                  "availability": "planned"
                },
                "smallest": {
                  "availability": "planned"
                }
              },
              "type": "enum",
              "values": [
                "original",
                "webp",
                "auto",
                "smallest"
              ]
            },
            "quality": {
              "default": 80,
              "depends_on": {
                "encoding_mode": "quality"
              },
              "description": "Compression quality (1 = smallest file, 100 = best quality). Active in `quality` mode (default); in `target_size` mode the quality is chosen automatically to hit the size budget, so this option is inactive.",
              "honored_on": [
                "same_format"
              ],
              "max": 100,
              "min": 1,
              "type": "integer"
            },
            "quality_preset": {
              "default": "good",
              "depends_on": {
                "encoding_mode": "auto_quality"
              },
              "description": "Named perceptual-quality target for `encoding_mode: auto_quality` (best > good > fair > low; `good` is the default). The worker runs a perceptual-target search and picks the encoder quality that meets the named tier \u2014 the wire carries the NAME only, so the tier\u2192target mapping stays worker-internal and calibration-tunable without a contract change. Requires `encoding_mode: auto_quality`. The achieved perceptual score is reported on the result as `metrics.measured_quality` (+ `metrics.quality_metric`).",
              "honored_on": [
                "same_format"
              ],
              "type": "enum",
              "values": [
                "best",
                "good",
                "fair",
                "low"
              ]
            },
            "target_size_bytes": {
              "depends_on": {
                "encoding_mode": "target_size"
              },
              "description": "Target output size in bytes (min 1024 = 1 KiB). The encoder searches for the highest quality that lands at or under this size; if the target can't be met even at the lowest quality, the closest result is returned (the result reports the chosen quality and whether the target was met). Requires `encoding_mode: target_size`.",
              "honored_on": [
                "same_format"
              ],
              "min": 1024,
              "type": "integer"
            },
            "width": {
              "description": "Target width in pixels (1-16384; width \u00d7 height must not exceed the group's maximum output area). Resizes the image as part of the operation (one transformed output, replacing the source). Optional; omit to keep the source dimensions.",
              "honored_on": [
                "same_format",
                "format_change"
              ],
              "max": 16384,
              "min": 1,
              "type": "integer"
            }
          }
        },
        "video": {
          "mimes": [
            "video/mp4",
            "video/webm",
            "video/quicktime"
          ],
          "options": {
            "audio_bitrate": {
              "depends_on": {
                "audio_codec": [
                  "aac",
                  "opus",
                  "vorbis"
                ]
              },
              "description": "Audio track bitrate in kbps. Only valid with an explicit re-encode audio_codec (aac/opus/vorbis): an explicit audio_bitrate with audio_codec `copy`/omitted is rejected; copy preserves the source bitrate. No default: a bitrate is only meaningful on an explicit re-encode.\n",
              "type": "enum",
              "value_type": "integer",
              "values": [
                64,
                96,
                128,
                192,
                256,
                320
              ]
            },
            "audio_codec": {
              "default": "copy",
              "description": "Audio codec. copy = passthrough (keep the source stream as-is; re-encode only on explicit request) \u2014 aac/opus/vorbis re-encode. Default is `copy`: on the same-container path (`output_format` original/absent) the source stream is already container-compatible, so copy is safe. When `output_format` RE-TARGETS the container, an absent/default audio_codec resolves to a container-valid codec via the facade (e.g. webm -> opus), NOT a blind copy of an incompatible stream \u2014 a DEFAULT is never rejected (the exact per-container resolution, incl. ogg vorbis-vs-opus, is an output_format stable-flip prerequisite). A caller-EXPLICIT codec the resolved container cannot carry (e.g. aac on a webm target) is rejected. See FORMAT.md \"Container-conditional defaults\".\n",
              "type": "enum",
              "values": [
                "aac",
                "opus",
                "vorbis",
                "copy"
              ]
            },
            "codec": {
              "default": "h264",
              "description": "Video codec. h264 = widest compatibility, h265/av1 = better compression but slower. The default (h264) is the MP4/MOV standard and is CONTAINER-CONDITIONAL against the RESOLVED output container (the explicit `output_format` if set, else the input container): for a container that cannot carry it (e.g. WebM, which carries VP8/VP9/ AV1) the server resolves a container-valid codec (WebM -> vp9). A DEFAULT value is never rejected \u2014 including an explicit `output_format: webm` left at the default `h264` (resolves to vp9, not rejected); only a caller-EXPLICIT incompatible codec is rejected. See FORMAT.md \"Container-conditional defaults\".\n",
              "type": "enum",
              "values": [
                "h264",
                "h265",
                "vp9",
                "av1"
              ]
            },
            "crf": {
              "default": 23,
              "depends_on": {
                "encoding_mode": "crf"
              },
              "description": "Constant Rate Factor (0 = lossless, 51 = worst, 23 = visually good). Only applies to CRF mode.",
              "max": 51,
              "min": 0,
              "type": "integer"
            },
            "encoding_mode": {
              "default": "crf",
              "description": "crf = constant quality (variable file size). target_size = constrained output file size (two-pass). target_size is LIVE for mp4/MOV output; for webm/ogg output and long-form the server returns `feature_not_available` (use crf there) until two-pass for this container ships.",
              "type": "enum",
              "values": [
                "crf",
                "target_size"
              ]
            },
            "faststart": {
              "default": true,
              "description": "Move the MP4/MOV moov atom to the front so the file starts playing before it is fully downloaded (progressive web playback). MP4/MOV only. CONTAINER-CONDITIONAL against the RESOLVED output container (the explicit `output_format` if set, else the input): the default (true) is silently inapplicable for a container with no moov atom (e.g. WebM) \u2014 that is NOT a rejection. Only a caller-EXPLICIT faststart:true on such a container is rejected. See FORMAT.md \"Container-conditional defaults\".\n",
              "type": "boolean"
            },
            "fit": {
              "default": "max",
              "depends_on": {
                "height": "set",
                "logic": "or",
                "width": "set"
              },
              "description": "Resize mode. max = fit within bounds (preserves ratio), crop/scale/pad = exact dimensions. Only applies when width or height is set.",
              "type": "enum",
              "values": [
                "max",
                "crop",
                "scale",
                "pad"
              ]
            },
            "fps": {
              "description": "Output frame rate",
              "max": 120,
              "min": 1,
              "type": "float"
            },
            "height": {
              "description": "Output height in pixels. Must be an even integer \u2265 2; odd values are rejected by the encoder.",
              "max": 4320,
              "min": 2,
              "type": "integer"
            },
            "output_format": {
              "default": "original",
              "description": "Output container. original = keep the input container (default, same-format compression). Non-`original` values re-target the container via the API \"compress+format\" facade (one canonicalized convert-with-size-cap pass); see FORMAT.md. When set, the RESOLVED target container determines the valid codec/audio_codec/faststart sets \u2014 an absent/default codec resolves to a container-valid one (e.g. webm -> vp9) and is never rejected; only a caller-EXPLICIT incompatible value is rejected.",
              "per_value_availability": {
                "mp4": {
                  "availability": "planned"
                },
                "ogg": {
                  "availability": "planned"
                },
                "webm": {
                  "availability": "planned"
                }
              },
              "type": "enum",
              "values": [
                "original",
                "mp4",
                "webm",
                "ogg"
              ]
            },
            "preset": {
              "default": "medium",
              "description": "Encoding speed vs compression ratio trade-off",
              "type": "enum",
              "values": [
                "ultrafast",
                "superfast",
                "veryfast",
                "faster",
                "fast",
                "medium",
                "slow",
                "slower",
                "veryslow"
              ]
            },
            "target_size_bytes": {
              "depends_on": {
                "encoding_mode": "target_size"
              },
              "description": "Target output file size in bytes (min 1MiB). A two-pass encode lands the output at or under the target (a safety undershoot + audio reserve apply). mp4/MOV output only \u2014 webm/ogg + long-form return `feature_not_available` until two-pass for this container ships.",
              "min": 1048576,
              "type": "integer"
            },
            "trim_end": {
              "description": "Trim from end in seconds (absent = to the end). Removes this many seconds from the end; with `trim_start` the kept clip is [`trim_start`, input_duration \u2212 `trim_end`]. See `trim_start` (short-form only; long-form is not supported).\n",
              "min": 0,
              "type": "float"
            },
            "trim_start": {
              "description": "Trim from beginning in seconds (absent = from the start). The clip is cut before encoding, with the same from-end semantic as convert/merge: the kept clip is [`trim_start`, input_duration \u2212 `trim_end`]. SHORT-FORM only \u2014 for long-form video this is not supported (trim a short clip, or chain a `split`); see the compress.video.trim.longform constraint.\n",
              "min": 0,
              "type": "float"
            },
            "width": {
              "description": "Output width in pixels. Must be an even integer \u2265 2; odd values are rejected by the encoder.",
              "max": 7680,
              "min": 2,
              "type": "integer"
            }
          },
          "processing_class": {
            "long_form": {
              "availability": "beta",
              "constraints": {
                "max_input_duration": "PT12H",
                "max_input_size_bytes": 5000000000,
                "max_output_size_bytes": 5000000000
              },
              "per_tier_constraints": {
                "enterprise": {
                  "max_input_size_bytes": 120000000000,
                  "max_output_size_bytes": 120000000000
                },
                "max": {
                  "max_input_size_bytes": 50000000000,
                  "max_output_size_bytes": 50000000000
                }
              },
              "required_tier": "pro"
            },
            "short_form": {
              "availability": "stable",
              "constraints": {
                "max_input_duration": "PT5M",
                "max_input_size_bytes": 524288000,
                "max_output_size_bytes": 524288000
              }
            }
          }
        }
      }
    },
    "convert": {
      "default": false,
      "description": "Convert file to a different format",
      "input_model": "single",
      "mime_groups": {
        "audio": {
          "mimes": [
            "audio/mpeg",
            "audio/aac",
            "audio/ogg",
            "audio/flac",
            "audio/wav",
            "audio/mp4"
          ],
          "options": {
            "bitrate": {
              "default": 128,
              "depends_on": {
                "output_format": [
                  "mp3",
                  "aac",
                  "ogg",
                  "opus"
                ]
              },
              "description": "Output bitrate in kbps (lossy formats only \u2014 mp3/aac/ogg/opus)",
              "type": "enum",
              "value_type": "integer",
              "values": [
                64,
                96,
                128,
                192,
                256,
                320
              ]
            },
            "output_format": {
              "description": "Target format",
              "required": true,
              "type": "enum",
              "values": [
                "mp3",
                "aac",
                "ogg",
                "opus",
                "flac",
                "wav"
              ]
            }
          }
        },
        "document_pdf": {
          "mimes": [
            "application/pdf"
          ],
          "options": {
            "dpi": {
              "default": 150,
              "description": "Render resolution in DPI",
              "max": 600,
              "min": 72,
              "type": "integer"
            },
            "output_format": {
              "description": "Target image format (renders PDF pages as images)",
              "required": true,
              "type": "enum",
              "values": [
                "png",
                "jpeg"
              ]
            },
            "pages": {
              "default": "1",
              "description": "Page selection (e.g. '1-5,8'). Each resolved page produces a\nseparate output file. **Maximum 200 resolved entries per\nrequest** \u2014 aligns with the `OperationResult.outputs[]`\n`maxItems: 200` cap. Expressions resolving to more than 200\nentries (e.g. `'1-201'`, `'1-100,102-205'`) are out of\ncontract. Duplicate or overlapping entries (e.g. `'1,1'`,\n`'1-100,50-150'`) are counted toward the cap as parsed;\ndeduplication, if any, is a runtime concern and is NOT part\nof this contract.\n",
              "type": "string"
            }
          }
        },
        "image": {
          "max_output_pixels": 16000000,
          "mimes": [
            "image/jpeg",
            "image/png",
            "image/webp",
            "image/tiff",
            "image/bmp",
            "image/vnd.microsoft.icon",
            "image/avif",
            "image/heic",
            "image/heif"
          ],
          "options": {
            "auto_orient": {
              "default": true,
              "description": "Bake EXIF orientation into the pixels (honour the camera/device rotation tag) so the output displays upright everywhere, then drop the now-redundant orientation tag. Default `true`; no-op for inputs without an orientation tag. The format-change re-encodes from pixels, so other (non-orientation) metadata is not carried across.",
              "honored_on": [
                "format_change"
              ],
              "type": "boolean"
            },
            "background": {
              "depends_on": {
                "output_format": "jpeg"
              },
              "description": "Background colour as a 6-digit hex (`#RRGGBB`, e.g. `#ffffff`) for transparent images converted to JPEG (the alpha-flatten fill colour when converting to JPEG). HEX ONLY: a non-hex value (e.g. a CSS named colour) is rejected rather than silently falling back.",
              "honored_on": [
                "format_change"
              ],
              "pattern": "^#[0-9a-fA-F]{6}$",
              "type": "string"
            },
            "color_profile": {
              "default": "keep",
              "description": "Colour-profile handling \u2014 a dedicated control, separate from `metadata` so that stripping metadata never silently shifts colours. `keep` (default) = preserve your colours: embed the ICC profile where the target format can carry one, otherwise convert to sRGB and bake the colours in so they survive (never a silent drop); `srgb` = convert to sRGB (transform pixels to sRGB, emit untagged \u2014 no embedded ICC, the web default; CMYK/grayscale best-effort); `strip` = drop the profile. The worker reads the input ICC for every accepted raster input (JPEG/PNG/WebP/TIFF, plus HEIF/AVIF via the ISO-BMFF `colr` box).",
              "honored_on": [
                "format_change"
              ],
              "type": "enum",
              "values": [
                "keep",
                "srgb",
                "strip"
              ]
            },
            "fit": {
              "default": "max",
              "depends_on": {
                "height": "set",
                "logic": "or",
                "width": "set"
              },
              "description": "Resize mode. `max` (default) = fit within the given bounds, preserving aspect ratio and never upscaling (caps at the source dimensions \u2014 this is the \"no-enlarge\" behaviour). `crop`/`scale` = exact dimensions (`scale` deliberately enlarges past the source). Applies only when width or height is set.",
              "honored_on": [
                "format_change"
              ],
              "type": "enum",
              "values": [
                "max",
                "crop",
                "scale"
              ]
            },
            "height": {
              "description": "Target height in pixels (1-16384; width \u00d7 height must not exceed the group's maximum output area). Not honored for SVG input. See `width`.",
              "honored_on": [
                "format_change"
              ],
              "max": 16384,
              "min": 1,
              "type": "integer"
            },
            "metadata": {
              "availability": "planned",
              "default": "strip",
              "description": "Metadata handling across a format change. `strip` = remove all EXIF/IPTC/XMP (default), `keep` = preserve. `planned` \u2014 metadata is not yet preserved across a format change. The honoured target subset may narrow (avif/heic targets are re-encoded and cannot keep metadata).",
              "honored_on": [
                "format_change"
              ],
              "type": "enum",
              "values": [
                "strip",
                "keep"
              ]
            },
            "output_format": {
              "description": "Target format",
              "required": true,
              "type": "enum",
              "values": [
                "jpeg",
                "png",
                "webp",
                "avif",
                "gif",
                "tiff"
              ]
            },
            "quality": {
              "default": 85,
              "depends_on": {
                "output_format": [
                  "jpeg",
                  "webp",
                  "avif"
                ]
              },
              "description": "Output quality for lossy formats",
              "honored_on": [
                "format_change"
              ],
              "max": 100,
              "min": 1,
              "type": "integer"
            },
            "width": {
              "description": "Target width in pixels (1-16384; width \u00d7 height must not exceed the group's maximum output area). Resizes the image before encoding (one transformed output). NOT honored for SVG input. Optional; omit to keep the source dimensions.",
              "honored_on": [
                "format_change"
              ],
              "max": 16384,
              "min": 1,
              "type": "integer"
            }
          }
        },
        "image_gif": {
          "max_output_pixels": 16000000,
          "mimes": [
            "image/gif"
          ],
          "options": {
            "auto_orient": {
              "default": true,
              "depends_on": {
                "output_format": [
                  "jpeg",
                  "png",
                  "webp",
                  "avif",
                  "gif",
                  "tiff"
                ]
              },
              "description": "Bake EXIF orientation into the pixels so the output displays upright everywhere. Default `true`; no-op for inputs without an orientation tag (a GIF carries none, so this is a no-op for GIF input). Scoped to the STILL-IMAGE targets only \u2014 the video targets (mp4/webm) carry no EXIF orientation.",
              "honored_on": [
                "format_change"
              ],
              "type": "boolean"
            },
            "background": {
              "depends_on": {
                "output_format": "jpeg"
              },
              "description": "Background colour as a 6-digit hex (`#RRGGBB`) for a transparent GIF flattened to JPEG (alpha-flatten). HEX ONLY \u2014 a non-hex value is rejected (see convert.image.background).",
              "honored_on": [
                "format_change"
              ],
              "pattern": "^#[0-9a-fA-F]{6}$",
              "type": "string"
            },
            "color_profile": {
              "default": "keep",
              "depends_on": {
                "output_format": [
                  "jpeg",
                  "png",
                  "webp",
                  "avif",
                  "gif",
                  "tiff"
                ]
              },
              "description": "Colour-profile handling \u2014 `keep` (default) = preserve your colours (embed where the target carries an ICC, else bake to sRGB; a no-op for the GIF input itself, which carries no profile); `srgb` = convert to sRGB (transform pixels to sRGB, emit untagged \u2014 no embedded ICC, the web default); `strip` = drop the profile. Scoped to the STILL-IMAGE targets only \u2014 the video targets (mp4/webm) carry no colour profile, so this is not accepted for a gif\u2192video output.",
              "honored_on": [
                "format_change"
              ],
              "type": "enum",
              "values": [
                "keep",
                "srgb",
                "strip"
              ]
            },
            "fit": {
              "default": "max",
              "depends_on": {
                "height": "set",
                "logic": "or",
                "width": "set"
              },
              "description": "Resize mode. `max` (default) = fit within bounds, preserving aspect ratio and never upscaling; `crop`/`scale` = exact dimensions. Applies only when width or height is set \u2014 and width/height are still-image-only here, so fit is transitively unreachable for the mp4/webm video targets.",
              "honored_on": [
                "format_change"
              ],
              "type": "enum",
              "values": [
                "max",
                "crop",
                "scale"
              ]
            },
            "height": {
              "depends_on": {
                "output_format": [
                  "jpeg",
                  "png",
                  "webp",
                  "avif",
                  "gif",
                  "tiff"
                ]
              },
              "description": "Target height in pixels (1-16384; width \u00d7 height must not exceed the group's maximum output area). Scoped to the still-image targets \u2014 see `width`.",
              "honored_on": [
                "format_change"
              ],
              "max": 16384,
              "min": 1,
              "type": "integer"
            },
            "output_format": {
              "description": "Target format. The still-image targets (jpeg/png/webp/avif/gif/tiff) take the first frame / rasterise. `mp4` / `webm` are the GIF\u2192VIDEO transcode targets \u2014 the animated GIF becomes a muted, play-once video (`mp4` = H.264, plays everywhere; `webm` = VP9, smaller), typically far smaller than the original GIF. GIFs are silent (no audio stream); the GIF infinite-loop becomes a playback attribute (`<video loop>`) the player sets, NOT baked into the file.",
              "required": true,
              "type": "enum",
              "values": [
                "jpeg",
                "png",
                "webp",
                "avif",
                "gif",
                "tiff",
                "mp4",
                "webm"
              ]
            },
            "quality": {
              "default": 85,
              "depends_on": {
                "output_format": [
                  "jpeg",
                  "webp",
                  "avif"
                ]
              },
              "description": "Output quality for the lossy STILL-IMAGE targets (jpeg/webp/avif). The video targets (mp4/webm) use a sensible default encode quality in v1 \u2014 there is no exposed CRF knob yet (may be added later).",
              "honored_on": [
                "format_change"
              ],
              "max": 100,
              "min": 1,
              "type": "integer"
            },
            "width": {
              "depends_on": {
                "output_format": [
                  "jpeg",
                  "png",
                  "webp",
                  "avif",
                  "gif",
                  "tiff"
                ]
              },
              "description": "Target width in pixels (1-16384; width \u00d7 height must not exceed the group's maximum output area). Scoped to the STILL-IMAGE targets only \u2014 resizing while transcoding to the mp4/webm video targets is not yet supported, so width/height are not accepted for the video targets.",
              "honored_on": [
                "format_change"
              ],
              "max": 16384,
              "min": 1,
              "type": "integer"
            }
          }
        },
        "image_svg": {
          "mimes": [
            "image/svg+xml"
          ],
          "options": {
            "background": {
              "depends_on": {
                "output_format": "jpeg"
              },
              "description": "Background colour as a 6-digit hex (`#RRGGBB`) for transparent SVG rasterised to JPEG (alpha-flatten). HEX ONLY \u2014 a non-hex value is rejected (see convert.image.background).",
              "honored_on": [
                "format_change"
              ],
              "pattern": "^#[0-9a-fA-F]{6}$",
              "type": "string"
            },
            "output_format": {
              "description": "Target raster format (SVG is rasterised, then encoded).",
              "required": true,
              "type": "enum",
              "values": [
                "jpeg",
                "png",
                "webp",
                "avif",
                "gif",
                "tiff"
              ]
            },
            "quality": {
              "default": 85,
              "depends_on": {
                "output_format": [
                  "jpeg",
                  "webp",
                  "avif"
                ]
              },
              "description": "Output quality for lossy formats",
              "honored_on": [
                "format_change"
              ],
              "max": 100,
              "min": 1,
              "type": "integer"
            }
          }
        },
        "video": {
          "mimes": [
            "video/mp4",
            "video/webm",
            "video/ogg"
          ],
          "options": {
            "crf": {
              "default": 23,
              "depends_on": {
                "output_format": [
                  "mp4",
                  "webm",
                  "ogg",
                  "mov",
                  "mkv"
                ]
              },
              "description": "CRF (Constant Rate Factor): 0 = lossless/best, 51 = worst. Lower = higher quality + larger file (the inverse of image `quality`). Video outputs only \u2014 NOT applicable to a `gif` output (frame extraction has no CRF). An explicit `crf` on a `gif` output is rejected as `invalid_options` at workflow-create (the API enforces this `depends_on`); FE/SDK prune it pre-submit.\n",
              "max": 51,
              "min": 0,
              "type": "integer"
            },
            "dither": {
              "default": "bayer",
              "depends_on": {
                "output_format": "gif"
              },
              "description": "GIF dithering method. `bayer` is a good size/quality balance; `none` is smallest (may band).\n",
              "type": "enum",
              "values": [
                "none",
                "bayer",
                "floyd_steinberg",
                "sierra2",
                "sierra2_4a"
              ]
            },
            "fps": {
              "default": 15,
              "depends_on": {
                "output_format": "gif"
              },
              "description": "Output frame rate for video \u2192 GIF. Lower = fewer frames = smaller file. Video \u2192 GIF only.\n",
              "max": 50,
              "min": 1,
              "type": "float"
            },
            "loop": {
              "default": 0,
              "depends_on": {
                "output_format": "gif"
              },
              "description": "GIF loop count: 0 = infinite (default), N > 0 = loop N times, -1 = play once.\n",
              "min": -1,
              "type": "integer"
            },
            "max_colors": {
              "default": 256,
              "depends_on": {
                "output_format": "gif"
              },
              "description": "GIF palette size (the palette generation max colours). Fewer colors = smaller file.\n",
              "max": 256,
              "min": 2,
              "type": "integer"
            },
            "output_format": {
              "description": "Target format. The video codec is fixed by the container (mp4->H.264, webm->VP9, ogg->Theora, gif->palette, mov->H.264, mkv->H.264) \u2014 not caller-selectable.",
              "required": true,
              "type": "enum",
              "values": [
                "mp4",
                "webm",
                "ogg",
                "gif",
                "mov",
                "mkv"
              ]
            },
            "trim_end": {
              "description": "Trim from end in seconds (absent = to the end of the clip). Removes this many seconds from the end; combined with `trim_start` the kept clip is [`trim_start`, input_duration \u2212 `trim_end`]. Live \u2014 see `trim_start`.\n",
              "min": 0,
              "type": "float"
            },
            "trim_start": {
              "description": "Trim from beginning in seconds (absent = from the start). The clip is cut before transcoding \u2014 the user's \"trim a video then make a GIF\" path. The kept clip is [`trim_start`, input_duration \u2212 `trim_end`]. Live for all video outputs.\n",
              "min": 0,
              "type": "float"
            },
            "width": {
              "default": 640,
              "depends_on": {
                "output_format": "gif"
              },
              "description": "Maximum output width in pixels \u2014 a downscale-only CAP (a source narrower than this is left untouched; height is aspect-preserved). Smaller = smaller file.\n",
              "max": 4096,
              "min": 1,
              "type": "integer"
            }
          },
          "processing_class": {
            "long_form": {
              "availability": "planned",
              "constraints": {
                "max_input_duration": "PT12H",
                "max_input_size_bytes": 5000000000,
                "max_output_size_bytes": 5000000000
              },
              "per_tier_constraints": {
                "enterprise": {
                  "max_input_size_bytes": 120000000000,
                  "max_output_size_bytes": 120000000000
                },
                "max": {
                  "max_input_size_bytes": 50000000000,
                  "max_output_size_bytes": 50000000000
                }
              },
              "required_tier": "pro"
            },
            "short_form": {
              "availability": "stable",
              "constraints": {
                "max_input_duration": "PT5M",
                "max_input_size_bytes": 524288000,
                "max_output_size_bytes": 524288000
              }
            }
          }
        }
      }
    },
    "custom_luma": {
      "availability": "planned",
      "default": false,
      "description": "Apply a caller-uploaded luma matte (transition-mask) over a base\nvideo to produce a custom luma-matte transition effect. Inspired\nby Cloudinary's deprecated luma-matte custom transitions and\nintended as a paid-tier (`pro`+) creative feature.\n\n**NOT** related to the `xfade=custom` expression-mode transition\nparameter. The `merge.transition.custom` value is tagged `planned`\nas an advisory that points callers at this dedicated operation for\ncaller-uploaded luma-matte transitions.\n\nMulti-input: exactly one input with `role: base` (the source\nvideo being transitioned) + exactly one input with\n`role: transition_mask` (the luma matte \u2014 a video whose pixel\nluminance drives the transition reveal \u2014 bright = revealed,\ndark = held). Each input is imported\nexternally, referenced from a vault connection, or an upstream\n`job_output` (uploads are NOT referenced directly inside inputs[]).\nAn uploaded base or mask enters via a `passthrough` source job\nreferenced by `{ type: job_output, from: <id> }` (per ticket\n4som89Uh).\n\n`planned` \u2014 not yet supported.\n",
      "input_model": "multi",
      "max_inputs": 2,
      "mime_groups": {
        "video": {
          "mimes": [
            "video/mp4",
            "video/webm"
          ],
          "options": {
            "duration": {
              "default": 1.0,
              "description": "Transition duration in seconds. The mask is time-stretched\nor trimmed to this duration when applied.\n",
              "max": 30.0,
              "min": 0.1,
              "type": "float"
            },
            "mask_blur": {
              "default": 0.0,
              "description": "Gaussian blur radius (pixels) applied to the luma matte\nbefore compositing. Softens the transition edge. Default\n0.0 = no blur. Larger values produce a more diffused\ntransition.\n",
              "max": 10.0,
              "min": 0.0,
              "type": "float"
            },
            "mask_contrast": {
              "default": 1.0,
              "description": "Contrast multiplier applied to the luma matte before\ncompositing. Sharpens (>1) or softens (<1) the\ntransition edge. Default 1.0 = no contrast adjustment.\n",
              "max": 4.0,
              "min": 0.0,
              "type": "float"
            },
            "mask_invert": {
              "default": false,
              "description": "When true, inverts the luma matte (bright \u2194 dark). Lets\ncallers reuse a single mask asset for both reveal and\nhide directions.\n",
              "type": "boolean"
            },
            "start_offset": {
              "default": 0.0,
              "description": "Offset (seconds) from the base clip start at which the\ntransition begins. The transition runs for `duration`\nseconds starting at `start_offset`.\n",
              "min": 0.0,
              "type": "float"
            }
          },
          "processing_class": {
            "long_form": {
              "availability": "planned",
              "constraints": {
                "max_input_duration": "PT12H",
                "max_input_size_bytes": 5000000000,
                "max_output_size_bytes": 5000000000
              },
              "per_tier_constraints": {
                "enterprise": {
                  "max_input_size_bytes": 120000000000,
                  "max_output_size_bytes": 120000000000
                },
                "max": {
                  "max_input_size_bytes": 50000000000,
                  "max_output_size_bytes": 50000000000
                }
              },
              "required_tier": "pro"
            },
            "short_form": {
              "availability": "planned",
              "constraints": {
                "max_input_duration": "PT5M",
                "max_input_size_bytes": 524288000,
                "max_output_size_bytes": 524288000
              },
              "required_tier": "pro"
            }
          }
        }
      },
      "min_inputs": 2,
      "per_role_cardinality": {
        "base": {
          "max": 1,
          "min": 1
        },
        "transition_mask": {
          "max": 1,
          "min": 1
        }
      },
      "required_tier": "pro",
      "sole_op": true
    },
    "image_watermark": {
      "default": false,
      "description": "Apply an image overlay onto a base media\nasset. Multi-input: exactly one input with role: base (the source\nasset) + exactly one with role: overlay (the watermark). Each input\nis an external_import handle, a vault\nconnection, or an upstream `job_output` (uploads are NOT referenced\ndirectly inside inputs[]). To use an uploaded base or overlay, feed\nit through a `passthrough` source job and reference that job via\n`{ type: job_output, from: <id> }` (per ticket 4som89Uh).\n\nStable today for static-image bases (image/jpeg, image/png,\nimage/webp); animated GIF bases are advertised as `planned` via\nthe parallel `image_gif` mime_group \u2014 schema is contract-defined,\ndispatch returns `feature_not_available` (422) until backend\nsupport ships.\n\nTIFF (image/tiff) and BMP (image/bmp) bases are now `stable` too\n(flipped planned->stable 2026-06-30 on the lambdas staging proof,\nUANjD38A / #296) via the parallel `image_tiff` / `image_bmp`\nmime_groups. Because watermark output mirrors the input format\n(`produces: same_as_input`), advertising a base format is only honest\nwhen the worker can both decode AND encode it \u2014 which holds for\nTIFF/BMP but not for HEIC/AVIF (no encoder) or SVG (cannot re-emit svg\nfrom a rasterised composite); those stay out of scope.\n\n**Video bases are NOT supported by `image_watermark`.** Use the\ndedicated `video_watermark` operation\n(`schemas/operations/video_watermark.yaml`), which overlays and\nre-encodes the video.\n\n**Audio is explicitly NOT supported.** For audio overlay (DJ tags,\npodcast intros, jingles), use the `audio_overlay` operation\n(declared in schemas/operations/audio_overlay.yaml at\n`availability: planned`).\n\nSource capped at 100 megapixels for image bases; per-frame caps for\nanimated GIF bases will be defined when the corresponding backend\nsupport ships.\n",
      "features": {
        "multi_overlay_stack": {
          "availability": "stable",
          "description": "Stack up to 8 overlay inputs on one base image, each with its own\nplacement via the `overlays[]` array option (index-aligned to the\noverlay-role sources; see the `image` group's `overlays` option).\n`overlays[]` is mutually exclusive with the flat single-overlay options,\nwhich remain the 1-overlay back-compat path.\n\n**Base-format scope: multi-overlay (`overlays[]`) is on the `image`\ngroup (jpeg/png/webp) ONLY** \u2014 the format the worker proved. The\nstable `image_tiff` / `image_bmp` bases support the SINGLE-overlay flat\noptions but NOT `overlays[]` yet (multi-overlay on those bases needs its\nown worker proof \u2014 a base-format extension, separate from the group's\nstable single-overlay flip). GIF / video bases remain out of scope.\n\nThe op-level `input` max 9 / `per_role_cardinality` overlay `{1,8}` is\nthe MAXIMUM (the jpeg/png/webp base); input-cardinality has no per-mime-\ngroup primitive, so a `>1`-overlay request on a tiff/bmp base \u2014 which\nhas no `overlays[]` to place the extra overlays \u2014 is rejected as\n`invalid_options` (API + worker). Effective per-base overlay count:\njpeg/png/webp = 1\u20138, tiff/bmp = 1.\n"
        }
      },
      "input_model": "multi",
      "max_inputs": 9,
      "mime_groups": {
        "image": {
          "mimes": [
            "image/jpeg",
            "image/png",
            "image/webp"
          ],
          "options": {
            "anchor": {
              "default": "bottom_right",
              "description": "9-grid anchor position on the base image. Combined with\nmargin_x and margin_y for fine-grained offset from the\nanchor point.\n",
              "type": "enum",
              "values": [
                "top_left",
                "top_center",
                "top_right",
                "center_left",
                "center",
                "center_right",
                "bottom_left",
                "bottom_center",
                "bottom_right"
              ]
            },
            "margin_x": {
              "default": "0px",
              "description": "Horizontal offset from the anchor as either pixels (e.g.\n\"40px\") or percentage of base image width (e.g. \"5%\").\nDirection is anchor-relative \u2014 positive moves toward image\ncentre.\n\n**`anchor: center` exception.** When the anchor is `center`\nthe centre is the centre \u2014 there is no anchor-relative\ndirection. Non-zero `margin_x` (anything other than the\ndefault `\"0px\"` or the equivalent `\"0%\"`) combined with\n`anchor: center` is rejected by the API at workflow-create\ntime as `invalid_options` (HTTP 422). To nudge a\nroughly-centred overlay, pick a peripheral anchor (e.g.\n`top_left`) and use `margin_x` / `margin_y` to position it.\n",
              "pattern": "^\\d+(\\.\\d+)?(px|%)$",
              "type": "string"
            },
            "margin_y": {
              "default": "0px",
              "description": "Vertical offset from the anchor as either pixels (e.g.\n\"20px\") or percentage of base image height (e.g. \"3%\").\nDirection is anchor-relative \u2014 positive moves toward image\ncentre.\n\n**`anchor: center` exception.** Same rejection rule as\n`margin_x` \u2014 non-zero `margin_y` with `anchor: center` is\nrejected by the API at workflow-create time as\n`invalid_options` (HTTP 422).\n",
              "pattern": "^\\d+(\\.\\d+)?(px|%)$",
              "type": "string"
            },
            "opacity": {
              "default": 0.5,
              "description": "Overlay opacity (0 = fully transparent, 1 = fully opaque).\nClamped to range.\n",
              "max": 1.0,
              "min": 0.0,
              "type": "float"
            },
            "overlay_width": {
              "description": "Optional overlay width as either pixels (e.g. \"200px\") or\npercentage of base image width (e.g. \"30%\"). Aspect ratio is\npreserved. Omit to use the original overlay size.\n",
              "pattern": "^\\d+(\\.\\d+)?(px|%)$",
              "type": "string"
            },
            "overlays": {
              "description": "Per-overlay placement for the multi-overlay stack \u2014 one entry per\noverlay source, INDEX-ALIGNED: `overlays[i]` applies to overlay\nsource `i`. Stacks up to 8 overlays on one base\n(`features.multi_overlay_stack`). `len(overlays)` MUST equal the\nnumber of overlay-role sources (worker-enforced; `invalid_options`\notherwise).\n\n**Z-order = array index (painter's order, bottom \u2192 top).** Overlays\nare composited in array/source order: `overlays[0]` first (bottom),\nthen `overlays[1]`, \u2026 `overlays[N-1]` LAST (on top). Where two\noverlays overlap, the higher index wins. There is no per-overlay\n`z` field \u2014 the index IS the z-order. (Deterministic in the worker;\nidentical per-frame for animated-WebP bases.)\n\n**Mutually exclusive with the flat single-overlay options**\n(`anchor` / `margin_x` / `margin_y` / `opacity` / `overlay_width`):\na request that sets BOTH `overlays[]` and any flat option is\nrejected as `invalid_options` (API create-validation + worker) \u2014\nthere is no silent precedence. Use the flat options for the\nsingle-overlay path, `overlays[]` for the multi path.\n\nLIVE (flipped `planned` -> stable 2026-07-01 on the lambdas worker\nproof, co-landed with `input` max 2 -> 9 + `per_role_cardinality`\noverlay `{1,8}` + api's overlays[] validation & FNA-gate removal).\nThe single-overlay FLAT options remain the 1-overlay back-compat path.\n",
              "items": {
                "properties": {
                  "anchor": {
                    "default": "bottom_right",
                    "description": "9-grid anchor position on the base image (per-overlay; see the flat `anchor`).",
                    "enum": [
                      "top_left",
                      "top_center",
                      "top_right",
                      "center_left",
                      "center",
                      "center_right",
                      "bottom_left",
                      "bottom_center",
                      "bottom_right"
                    ],
                    "type": "string"
                  },
                  "margin_x": {
                    "default": "0px",
                    "description": "Horizontal offset from the anchor (px or % of base width), anchor-relative (see the flat `margin_x`).",
                    "pattern": "^\\d+(\\.\\d+)?(px|%)$",
                    "type": "string"
                  },
                  "margin_y": {
                    "default": "0px",
                    "description": "Vertical offset from the anchor (px or % of base height), anchor-relative (see the flat `margin_y`).",
                    "pattern": "^\\d+(\\.\\d+)?(px|%)$",
                    "type": "string"
                  },
                  "opacity": {
                    "default": 0.5,
                    "description": "Overlay opacity (0 transparent .. 1 opaque), clamped.",
                    "maximum": 1,
                    "minimum": 0,
                    "type": "number"
                  },
                  "overlay_width": {
                    "description": "Optional overlay width (px or % of base width); aspect preserved. Omit for original size.",
                    "pattern": "^\\d+(\\.\\d+)?(px|%)$",
                    "type": "string"
                  }
                },
                "type": "object"
              },
              "maxItems": 8,
              "minItems": 1,
              "type": "array"
            }
          }
        },
        "image_bmp": {
          "mimes": [
            "image/bmp"
          ],
          "options": {
            "anchor": {
              "default": "bottom_right",
              "description": "9-grid anchor position on the base image. Combined with\nmargin_x and margin_y for fine-grained offset from the\nanchor point.\n",
              "type": "enum",
              "values": [
                "top_left",
                "top_center",
                "top_right",
                "center_left",
                "center",
                "center_right",
                "bottom_left",
                "bottom_center",
                "bottom_right"
              ]
            },
            "margin_x": {
              "default": "0px",
              "description": "Horizontal offset from the anchor in pixels or percentage of\nbase width. Anchor-relative; positive moves toward the image\ncentre.\n\n**`anchor: center` exception.** Same rejection rule as the\n`image` mime_group \u2014 non-zero `margin_x` with\n`anchor: center` is rejected by the API at workflow-create\ntime as `invalid_options` (HTTP 422).\n",
              "pattern": "^\\d+(\\.\\d+)?(px|%)$",
              "type": "string"
            },
            "margin_y": {
              "default": "0px",
              "description": "Vertical offset from the anchor in pixels or percentage of\nbase height. Anchor-relative; positive moves toward the image\ncentre.\n\n**`anchor: center` exception.** Same rejection rule as the\n`image` mime_group \u2014 non-zero `margin_y` with\n`anchor: center` is rejected by the API at workflow-create\ntime as `invalid_options` (HTTP 422).\n",
              "pattern": "^\\d+(\\.\\d+)?(px|%)$",
              "type": "string"
            },
            "opacity": {
              "default": 0.5,
              "description": "Overlay opacity (0 = fully transparent, 1 = fully opaque).\nClamped to range.\n",
              "max": 1.0,
              "min": 0.0,
              "type": "float"
            },
            "overlay_width": {
              "description": "Optional overlay width as pixels or percentage of base\nwidth. Aspect ratio preserved. Omit to use the original\noverlay size.\n",
              "pattern": "^\\d+(\\.\\d+)?(px|%)$",
              "type": "string"
            }
          }
        },
        "image_gif": {
          "availability": "planned",
          "mimes": [
            "image/gif"
          ],
          "options": {
            "anchor": {
              "default": "bottom_right",
              "description": "9-grid anchor position on the base GIF (applied uniformly\nacross frames). Same semantics as the `image` group.\n",
              "type": "enum",
              "values": [
                "top_left",
                "top_center",
                "top_right",
                "center_left",
                "center",
                "center_right",
                "bottom_left",
                "bottom_center",
                "bottom_right"
              ]
            },
            "margin_x": {
              "default": "0px",
              "description": "Horizontal offset from the anchor in pixels or percentage of\nbase width. Anchor-relative; positive moves toward the\nframe centre.\n\n**`anchor: center` exception.** Same rejection rule as the\n`image` mime_group \u2014 non-zero `margin_x` with\n`anchor: center` is rejected by the API at workflow-create\ntime as `invalid_options` (HTTP 422).\n",
              "pattern": "^\\d+(\\.\\d+)?(px|%)$",
              "type": "string"
            },
            "margin_y": {
              "default": "0px",
              "description": "Vertical offset from the anchor in pixels or percentage of\nbase height. Anchor-relative; positive moves toward the\nframe centre.\n\n**`anchor: center` exception.** Same rejection rule as the\n`image` mime_group \u2014 non-zero `margin_y` with\n`anchor: center` is rejected by the API at workflow-create\ntime as `invalid_options` (HTTP 422).\n",
              "pattern": "^\\d+(\\.\\d+)?(px|%)$",
              "type": "string"
            },
            "opacity": {
              "default": 0.5,
              "description": "Overlay opacity (0 = fully transparent, 1 = fully opaque).\nClamped to range.\n",
              "max": 1.0,
              "min": 0.0,
              "type": "float"
            },
            "overlay_width": {
              "description": "Optional overlay width as pixels or percentage of base\nwidth. Aspect ratio preserved. Omit to use the original\noverlay size.\n",
              "pattern": "^\\d+(\\.\\d+)?(px|%)$",
              "type": "string"
            }
          }
        },
        "image_tiff": {
          "mimes": [
            "image/tiff"
          ],
          "options": {
            "anchor": {
              "default": "bottom_right",
              "description": "9-grid anchor position on the base image. Combined with\nmargin_x and margin_y for fine-grained offset from the\nanchor point.\n",
              "type": "enum",
              "values": [
                "top_left",
                "top_center",
                "top_right",
                "center_left",
                "center",
                "center_right",
                "bottom_left",
                "bottom_center",
                "bottom_right"
              ]
            },
            "margin_x": {
              "default": "0px",
              "description": "Horizontal offset from the anchor in pixels or percentage of\nbase width. Anchor-relative; positive moves toward the image\ncentre.\n\n**`anchor: center` exception.** Same rejection rule as the\n`image` mime_group \u2014 non-zero `margin_x` with\n`anchor: center` is rejected by the API at workflow-create\ntime as `invalid_options` (HTTP 422).\n",
              "pattern": "^\\d+(\\.\\d+)?(px|%)$",
              "type": "string"
            },
            "margin_y": {
              "default": "0px",
              "description": "Vertical offset from the anchor in pixels or percentage of\nbase height. Anchor-relative; positive moves toward the image\ncentre.\n\n**`anchor: center` exception.** Same rejection rule as the\n`image` mime_group \u2014 non-zero `margin_y` with\n`anchor: center` is rejected by the API at workflow-create\ntime as `invalid_options` (HTTP 422).\n",
              "pattern": "^\\d+(\\.\\d+)?(px|%)$",
              "type": "string"
            },
            "opacity": {
              "default": 0.5,
              "description": "Overlay opacity (0 = fully transparent, 1 = fully opaque).\nClamped to range.\n",
              "max": 1.0,
              "min": 0.0,
              "type": "float"
            },
            "overlay_width": {
              "description": "Optional overlay width as pixels or percentage of base\nwidth. Aspect ratio preserved. Omit to use the original\noverlay size.\n",
              "pattern": "^\\d+(\\.\\d+)?(px|%)$",
              "type": "string"
            }
          }
        }
      },
      "min_inputs": 2,
      "per_role_cardinality": {
        "base": {
          "max": 1,
          "min": 1
        },
        "overlay": {
          "max": 8,
          "min": 1
        }
      },
      "sole_op": true
    },
    "merge": {
      "default": false,
      "description": "Combine multiple files into one",
      "input_model": "multi",
      "max_inputs": 10,
      "mime_groups": {
        "audio": {
          "mimes": [
            "audio/mpeg",
            "audio/aac",
            "audio/wav",
            "audio/ogg",
            "audio/flac"
          ],
          "options": {
            "crossfade_duration": {
              "default": 0,
              "depends_on": {
                "transition": "crossfade"
              },
              "description": "Crossfade duration in seconds",
              "max": 10.0,
              "min": 0,
              "type": "float"
            },
            "gap_duration": {
              "default": 0,
              "description": "Silence duration between clips in seconds",
              "max": 10.0,
              "min": 0,
              "type": "float"
            },
            "normalize_audio": {
              "default": true,
              "description": "Normalize audio levels across all clips",
              "type": "boolean"
            },
            "output_type": {
              "description": "Output format for audio merge (concatenation). Currently fixed at 'audio'. Optional \u2014 API injects this default post-validation so callers may omit it.",
              "required": false,
              "type": "enum",
              "values": [
                "audio"
              ]
            },
            "transition": {
              "default": "none",
              "description": "Transition between audio clips. `none` joins them with a\nhard cut; `crossfade` blends one into the next.\n",
              "type": "enum",
              "values": [
                "none",
                "crossfade"
              ]
            }
          },
          "per_input_options": {
            "crossfade_duration": {
              "depends_on": {
                "transition": "crossfade"
              },
              "description": "Override crossfade duration for this join point",
              "max": 10.0,
              "min": 0,
              "type": "float"
            },
            "gap_duration": {
              "description": "Override gap duration for this join point",
              "max": 10.0,
              "min": 0,
              "type": "float"
            },
            "transition": {
              "description": "Override transition for the join point preceding this\ninput. Audio-only catalog (acrossfade-backed); see\nFORMAT.md \u00a7\"Audio vs visual transition catalogs (merge)\".\n",
              "type": "enum",
              "values": [
                "none",
                "crossfade"
              ]
            },
            "trim_end": {
              "description": "Trim from end in seconds (per-input). Applied to this input\nBEFORE any transition/crossfade. Matches the `compress` trim\nsemantic. No `max` \u2014 the upper bound is the input's own\nduration, enforced at processing time, not statically bounded\nhere.\n",
              "min": 0,
              "type": "float"
            },
            "trim_start": {
              "description": "Trim from beginning in seconds (per-input). Applied to this\ninput BEFORE any transition/crossfade. Matches the\n`compress` trim semantic. No `max` \u2014 the upper bound is the\ninput's own duration, enforced at processing time, not statically\nbounded here.\n",
              "min": 0,
              "type": "float"
            }
          }
        },
        "image": {
          "mimes": [
            "image/jpeg",
            "image/png",
            "image/webp",
            "image/gif",
            "image/tiff",
            "image/bmp"
          ],
          "options": {
            "delay": {
              "default": 500,
              "depends_on": {
                "output_type": "gif"
              },
              "description": "Milliseconds between frames",
              "max": 5000,
              "min": 10,
              "type": "integer"
            },
            "duration_per_image": {
              "default": 3.0,
              "depends_on": {
                "output_type": "video"
              },
              "description": "Seconds each image is displayed",
              "max": 30.0,
              "min": 0.5,
              "type": "float"
            },
            "fps": {
              "default": 30,
              "depends_on": {
                "output_type": "video"
              },
              "description": "Output frame rate",
              "max": 60,
              "min": 1,
              "type": "integer"
            },
            "loop_count": {
              "default": 0,
              "depends_on": {
                "output_type": "gif"
              },
              "description": "Number of loops (0 = infinite)",
              "max": 100,
              "min": 0,
              "type": "integer"
            },
            "output_type": {
              "description": "Result format: animated GIF or slideshow video. No default \u2014 caller must pick explicitly (image/collage and document/PDF concat are not supported by V1 merge).",
              "required": true,
              "type": "enum",
              "values": [
                "gif",
                "video"
              ]
            },
            "transition": {
              "default": "none",
              "depends_on": {
                "output_type": "video"
              },
              "description": "Transition effect between images in the slideshow output.\n`none` means a hard cut with no transition. Some effects\nare still rolling out \u2014 check their availability.\n",
              "per_value_availability": {
                "circleclose": {
                  "availability": "beta"
                },
                "circlecrop": {
                  "availability": "beta"
                },
                "circleopen": {
                  "availability": "beta"
                },
                "custom": {
                  "availability": "planned"
                },
                "diagbl": {
                  "availability": "beta"
                },
                "diagtl": {
                  "availability": "beta"
                },
                "diagtr": {
                  "availability": "beta"
                },
                "distance": {
                  "availability": "experimental"
                },
                "hblur": {
                  "availability": "experimental"
                },
                "hlslice": {
                  "availability": "experimental"
                },
                "pixelize": {
                  "availability": "experimental"
                },
                "radial": {
                  "availability": "beta"
                },
                "rectcrop": {
                  "availability": "beta"
                },
                "slidedown": {
                  "availability": "beta"
                },
                "slideleft": {
                  "availability": "beta"
                },
                "slideright": {
                  "availability": "beta"
                },
                "slideup": {
                  "availability": "beta"
                },
                "smoothdown": {
                  "availability": "beta"
                },
                "smoothleft": {
                  "availability": "beta"
                },
                "smoothright": {
                  "availability": "beta"
                },
                "smoothup": {
                  "availability": "beta"
                },
                "squeezeh": {
                  "availability": "experimental"
                },
                "vlslice": {
                  "availability": "experimental"
                },
                "wipebl": {
                  "availability": "planned",
                  "eta": "2026-Q3"
                },
                "wipetl": {
                  "availability": "planned",
                  "eta": "2026-Q3"
                },
                "wipetr": {
                  "availability": "planned",
                  "eta": "2026-Q3"
                }
              },
              "type": "enum",
              "values": [
                "none",
                "fade",
                "fadeblack",
                "fadewhite",
                "dissolve",
                "wipeleft",
                "wiperight",
                "wipeup",
                "wipedown",
                "crossfade",
                "smoothleft",
                "smoothright",
                "smoothup",
                "smoothdown",
                "circleopen",
                "circleclose",
                "circlecrop",
                "rectcrop",
                "slideleft",
                "slideright",
                "slideup",
                "slidedown",
                "radial",
                "diagtl",
                "diagtr",
                "diagbl",
                "pixelize",
                "hlslice",
                "vlslice",
                "hblur",
                "distance",
                "squeezeh",
                "custom",
                "wipetl",
                "wipetr",
                "wipebl"
              ]
            },
            "transition_duration": {
              "default": 0.5,
              "depends_on": {
                "output_type": "video"
              },
              "description": "Duration of transition effect in seconds",
              "max": 5.0,
              "min": 0.1,
              "type": "float"
            },
            "video_format": {
              "default": "mp4",
              "depends_on": {
                "output_type": "video"
              },
              "description": "Output video container format",
              "type": "enum",
              "values": [
                "mp4",
                "webm"
              ]
            }
          }
        },
        "video": {
          "mimes": [
            "video/mp4",
            "video/webm",
            "video/quicktime"
          ],
          "options": {
            "codec": {
              "default": "h264",
              "depends_on": {
                "re_encode_mode": [
                  "auto",
                  "always"
                ]
              },
              "description": "Output codec when merge re-encodes. Inert when stream-copy\nconcat is selected (re_encode_mode=auto + compatible inputs).\n",
              "per_value_availability": {
                "av1": {
                  "availability": "beta"
                }
              },
              "type": "enum",
              "values": [
                "h264",
                "h265",
                "vp9",
                "av1"
              ]
            },
            "crf": {
              "default": 23,
              "depends_on": {
                "encoding_mode": "crf",
                "re_encode_mode": [
                  "auto",
                  "always"
                ]
              },
              "description": "Constant Rate Factor when encoding_mode=crf. Lower = higher quality / larger file.",
              "max": 51,
              "min": 0,
              "type": "integer"
            },
            "crossfade_duration": {
              "default": 1.0,
              "depends_on": {
                "transition": "crossfade"
              },
              "description": "Crossfade duration in seconds",
              "max": 5.0,
              "min": 0.1,
              "type": "float"
            },
            "encoding_mode": {
              "default": "crf",
              "depends_on": {
                "re_encode_mode": [
                  "auto",
                  "always"
                ]
              },
              "description": "CRF aims for a quality level and lets the file size vary;\ntarget_size aims for a fixed output file size. `target_size`\ncurrently works with `codec=h264` only \u2014 a request pairing it\nwith another codec is rejected as `invalid_options`.\n",
              "per_value_depends_on": {
                "target_size": {
                  "codec": [
                    "h264"
                  ]
                }
              },
              "type": "enum",
              "values": [
                "crf",
                "target_size"
              ]
            },
            "normalize_audio": {
              "default": true,
              "description": "Normalize audio levels across all clips",
              "type": "boolean"
            },
            "output_type": {
              "description": "Output format for video merge (concatenation). Currently fixed at 'video'. Optional \u2014 API injects this default post-validation so callers may omit it.",
              "required": false,
              "type": "enum",
              "values": [
                "video"
              ]
            },
            "preset": {
              "default": "medium",
              "depends_on": {
                "re_encode_mode": [
                  "auto",
                  "always"
                ]
              },
              "description": "Encoder speed/compression trade-off when re-encoding.",
              "type": "enum",
              "values": [
                "ultrafast",
                "superfast",
                "veryfast",
                "faster",
                "fast",
                "medium",
                "slow",
                "slower",
                "veryslow"
              ]
            },
            "re_encode_mode": {
              "default": "auto",
              "description": "Re-encode policy for video concat.\n- `auto`: probe inputs and choose stream-copy concat (when\n  codec/container/timebase/framerate/resolution/pixel-format\n  match) or full re-encode.\n- `always`: force re-encode (use when caller wants\n  normalised output options).\n- `never`: return 422 `REQUIRES_REENCODE` if inputs are not\n  concat-compatible.\nCompatibility algorithm is server-side and deliberately\nopaque (per plan v5 \u00a7F8.2). The actual path taken is\nreported on `OperationMetrics.re_encode_decision`.\n",
              "type": "enum",
              "values": [
                "auto",
                "always",
                "never"
              ]
            },
            "target_resolution": {
              "depends_on": {
                "re_encode_mode": [
                  "auto",
                  "always"
                ]
              },
              "description": "Output dimensions WxH (e.g. \"1920x1080\"). Omit to inherit from inputs.",
              "pattern": "^\\d+x\\d+$",
              "type": "string"
            },
            "target_size_bytes": {
              "depends_on": {
                "encoding_mode": "target_size",
                "re_encode_mode": [
                  "auto",
                  "always"
                ]
              },
              "description": "Target file size in bytes (minimum 1 MiB). Required when\n`encoding_mode=target_size`, which is h264-only.\n\nPractical floor: if the target works out to less than\n~100 kbps of video (after subtracting the ~160 kbps AAC audio\ntrack over the merged duration), the encoder floors it and the\nresulting file will not honour the requested `target_size_bytes`.\nPublishers targeting very small outputs should size accordingly.\n",
              "min": 1048576,
              "type": "integer"
            },
            "transition": {
              "default": "none",
              "description": "Default transition between clips. Some effects are still\nrolling out \u2014 check their availability.\n",
              "per_value_availability": {
                "circleclose": {
                  "availability": "beta"
                },
                "circlecrop": {
                  "availability": "beta"
                },
                "circleopen": {
                  "availability": "beta"
                },
                "custom": {
                  "availability": "planned"
                },
                "diagbl": {
                  "availability": "beta"
                },
                "diagtl": {
                  "availability": "beta"
                },
                "diagtr": {
                  "availability": "beta"
                },
                "distance": {
                  "availability": "experimental"
                },
                "hblur": {
                  "availability": "experimental"
                },
                "hlslice": {
                  "availability": "experimental"
                },
                "pixelize": {
                  "availability": "experimental"
                },
                "radial": {
                  "availability": "beta"
                },
                "rectcrop": {
                  "availability": "beta"
                },
                "slidedown": {
                  "availability": "beta"
                },
                "slideleft": {
                  "availability": "beta"
                },
                "slideright": {
                  "availability": "beta"
                },
                "slideup": {
                  "availability": "beta"
                },
                "smoothdown": {
                  "availability": "beta"
                },
                "smoothleft": {
                  "availability": "beta"
                },
                "smoothright": {
                  "availability": "beta"
                },
                "smoothup": {
                  "availability": "beta"
                },
                "squeezeh": {
                  "availability": "experimental"
                },
                "vlslice": {
                  "availability": "experimental"
                },
                "wipebl": {
                  "availability": "planned",
                  "eta": "2026-Q3"
                },
                "wipetl": {
                  "availability": "planned",
                  "eta": "2026-Q3"
                },
                "wipetr": {
                  "availability": "planned",
                  "eta": "2026-Q3"
                }
              },
              "type": "enum",
              "values": [
                "none",
                "fade",
                "fadeblack",
                "fadewhite",
                "dissolve",
                "wipeleft",
                "wiperight",
                "wipeup",
                "wipedown",
                "crossfade",
                "smoothleft",
                "smoothright",
                "smoothup",
                "smoothdown",
                "circleopen",
                "circleclose",
                "circlecrop",
                "rectcrop",
                "slideleft",
                "slideright",
                "slideup",
                "slidedown",
                "radial",
                "diagtl",
                "diagtr",
                "diagbl",
                "pixelize",
                "hlslice",
                "vlslice",
                "hblur",
                "distance",
                "squeezeh",
                "custom",
                "wipetl",
                "wipetr",
                "wipebl"
              ]
            }
          },
          "per_input_options": {
            "crossfade_duration": {
              "depends_on": {
                "transition": "crossfade"
              },
              "description": "Override crossfade duration for this join point",
              "max": 5.0,
              "min": 0.1,
              "type": "float"
            },
            "transition": {
              "description": "Override transition for the join point preceding this input.",
              "per_value_availability": {
                "circleclose": {
                  "availability": "beta"
                },
                "circlecrop": {
                  "availability": "beta"
                },
                "circleopen": {
                  "availability": "beta"
                },
                "custom": {
                  "availability": "planned"
                },
                "diagbl": {
                  "availability": "beta"
                },
                "diagtl": {
                  "availability": "beta"
                },
                "diagtr": {
                  "availability": "beta"
                },
                "distance": {
                  "availability": "experimental"
                },
                "hblur": {
                  "availability": "experimental"
                },
                "hlslice": {
                  "availability": "experimental"
                },
                "pixelize": {
                  "availability": "experimental"
                },
                "radial": {
                  "availability": "beta"
                },
                "rectcrop": {
                  "availability": "beta"
                },
                "slidedown": {
                  "availability": "beta"
                },
                "slideleft": {
                  "availability": "beta"
                },
                "slideright": {
                  "availability": "beta"
                },
                "slideup": {
                  "availability": "beta"
                },
                "smoothdown": {
                  "availability": "beta"
                },
                "smoothleft": {
                  "availability": "beta"
                },
                "smoothright": {
                  "availability": "beta"
                },
                "smoothup": {
                  "availability": "beta"
                },
                "squeezeh": {
                  "availability": "experimental"
                },
                "vlslice": {
                  "availability": "experimental"
                },
                "wipebl": {
                  "availability": "planned",
                  "eta": "2026-Q3"
                },
                "wipetl": {
                  "availability": "planned",
                  "eta": "2026-Q3"
                },
                "wipetr": {
                  "availability": "planned",
                  "eta": "2026-Q3"
                }
              },
              "type": "enum",
              "values": [
                "none",
                "fade",
                "fadeblack",
                "fadewhite",
                "dissolve",
                "wipeleft",
                "wiperight",
                "wipeup",
                "wipedown",
                "crossfade",
                "smoothleft",
                "smoothright",
                "smoothup",
                "smoothdown",
                "circleopen",
                "circleclose",
                "circlecrop",
                "rectcrop",
                "slideleft",
                "slideright",
                "slideup",
                "slidedown",
                "radial",
                "diagtl",
                "diagtr",
                "diagbl",
                "pixelize",
                "hlslice",
                "vlslice",
                "hblur",
                "distance",
                "squeezeh",
                "custom",
                "wipetl",
                "wipetr",
                "wipebl"
              ]
            },
            "trim_end": {
              "description": "Trim from end in seconds (per-input). Applied to this input\nBEFORE any transition/crossfade. Matches the `compress` trim\nsemantic. No `max` \u2014 the upper bound is the input's own\nduration, enforced at processing time, not statically bounded\nhere.\n",
              "min": 0,
              "type": "float"
            },
            "trim_start": {
              "description": "Trim from beginning in seconds (per-input). Applied to this\ninput BEFORE any transition/crossfade. Matches the\n`compress` trim semantic. No `max` \u2014 the upper bound is the\ninput's own duration, enforced at processing time, not statically\nbounded here.\n",
              "min": 0,
              "type": "float"
            }
          },
          "processing_class": {
            "long_form_re_encode": {
              "availability": "beta",
              "constraints": {
                "max_total_duration": "PT24H",
                "max_total_input_size_bytes": 5000000000
              },
              "per_tier_constraints": {
                "enterprise": {
                  "max_total_input_size_bytes": 120000000000
                },
                "max": {
                  "max_total_input_size_bytes": 50000000000
                }
              },
              "required_tier": "pro"
            },
            "short_form_concat": {
              "availability": "stable",
              "constraints": {
                "max_total_duration": "PT5M",
                "max_total_input_size_bytes": 1073741824
              }
            }
          }
        }
      },
      "min_inputs": 2,
      "sole_op": true
    },
    "passthrough": {
      "availability": "beta",
      "default": false,
      "description": "Inert lossless source operation. A single-input source job whose\nSOLE operation is `passthrough` emits its source bytes UNCHANGED \u2014\nno compression, no transformation, no backend dispatch. The API\nself-completes the job at workflow-create/publish time: the job's\nterminal output IS the upload's `{bucket, key}` unchanged.\n\n**Purpose.** Lets an uploaded file enter a multi-input operation\n(`merge` / `archive` / `image_watermark` / etc.) LOSSLESSLY. Since\nV2 narrows the input source to exclude upload-direct (a multi-input source\n\u2014 job_output / external_import / connection only), an upload that\nmust feed a multi-input op enters via a `passthrough` source job\nreferenced downstream by `{ type: job_output, from: <id> }`. This\npreserves billing / DAG / lineage that direct upload-in-inputs[]\nwould bypass.\n\n**Distinct from `operations: []`.** An empty `operations[]` on a\nsingle-input upload job KEEPS its implicit-compress meaning (see\n`POST /api/workflows` description). `passthrough` is the EXPLICIT\nlossless opt-out chosen via the existing rule \"non-empty\n`operations[]` without `compress` = compression opt-out\" \u2014 it is a\nsole, named operation, not an empty chain.\n\n**Never published to SNS.** Because the API self-completes the job\nat publish, `passthrough` is INTENTIONALLY absent from the AsyncAPI\nrouting enums (`OperationType`, `OperationRequestAttributes.operation_type`)\nand has no `ops-passthrough` queue. It never appears on the wire as\na request message.\n\nMedia-agnostic (no `mime_groups`): it accepts any uploaded MIME and\ncopies it verbatim, so there are no per-MIME options or constraints.\n`availability: beta` \u2014 the API self-completion path is LIVE (the\ninputs[]-narrowing + passthrough self-complete mechanism is deployed),\nso workflow-create accepts `passthrough` source jobs and MUST NOT\nreturn `feature_not_available`. Activated per ticket\n[`4som89Uh`](https://trello.com/c/4som89Uh) (planned\u2192beta flip;\nself-complete code deployed API-side).\n",
      "input_model": "single"
    },
    "render_variants": {
      "availability": "planned",
      "description": "Generate multiple resized / re-encoded image variants from a single source image in one operation (image fan-out).",
      "input_model": "single",
      "mime_groups": {
        "image": {
          "availability": "planned",
          "mimes": [
            "image/jpeg",
            "image/png",
            "image/webp"
          ],
          "options": {
            "targets": {
              "description": "One output variant per entry (max 20). Each variant is resized /\nre-encoded independently from the single decoded source; the\noperation succeeds or fails **atomically** \u2014 there are no partial\noutputs. Each result output echoes its target's `id` back as\n`target_id` so callers can map outputs to the variants they asked\nfor.\n\nDimensions: `fit: max` needs at least one of `width` / `height`\n(aspect preserved, output may be smaller than the box). `fit: crop`\nand `fit: scale` require BOTH `width` and `height` (they produce an\nexact-size output). A target whose dimensions exceed the source is\nrejected (`invalid_options`) unless `allow_upscale: true`.\n",
              "items": {
                "properties": {
                  "allow_upscale": {
                    "default": false,
                    "description": "When false (default), a target whose dimensions exceed the source is rejected (`invalid_options`). Set true to permit upscaling. fit max never upscales.",
                    "type": "boolean"
                  },
                  "background": {
                    "default": "#FFFFFF",
                    "description": "Background colour (hex #RRGGBB), applied only when flattening a transparent source into an opaque format (e.g. a PNG with alpha rendered to JPEG). Ignored otherwise.",
                    "pattern": "^#[0-9A-Fa-f]{6}$",
                    "type": "string"
                  },
                  "fit": {
                    "default": "max",
                    "description": "Resize mode. max = fit within the box, aspect preserved\n(output may be smaller); needs at least one dimension.\ncrop = fill and trim to exact width x height. scale = force\nexact width x height. crop and scale require both dimensions.\n",
                    "enum": [
                      "max",
                      "crop",
                      "scale"
                    ],
                    "type": "string"
                  },
                  "height": {
                    "description": "Target height in pixels. Required together with width for fit crop/scale; for fit max, at least one of width/height is required.",
                    "maximum": 16384,
                    "minimum": 1,
                    "type": "integer"
                  },
                  "id": {
                    "description": "Caller-assigned identifier, unique within the array\n(duplicate ids are rejected as `invalid_options`). Echoed\nverbatim as `target_id` on the matching result output \u2014 the\naddressing contract.\n",
                    "maxLength": 64,
                    "pattern": "^[A-Za-z0-9._-]+$",
                    "type": "string"
                  },
                  "output_format": {
                    "description": "Output format for this variant. When omitted, the source format is preserved.",
                    "enum": [
                      "jpeg",
                      "png",
                      "webp",
                      "avif"
                    ],
                    "type": "string"
                  },
                  "quality": {
                    "default": 85,
                    "description": "Encode quality (lossy formats only \u2014 jpeg / webp / avif). Accepted but ignored for png (lossless).",
                    "maximum": 100,
                    "minimum": 1,
                    "type": "integer"
                  },
                  "width": {
                    "description": "Target width in pixels. Required together with height for fit crop/scale; for fit max, at least one of width/height is required.",
                    "maximum": 16384,
                    "minimum": 1,
                    "type": "integer"
                  }
                },
                "required": [
                  "id"
                ],
                "type": "object"
              },
              "maxItems": 20,
              "minItems": 1,
              "required": true,
              "type": "array"
            }
          }
        }
      }
    },
    "split": {
      "availability": "beta",
      "default": false,
      "description": "Fan one input file into N outputs across GIF, PDF, audio, and\nvideo MIME families. Single-input \u2014 one input file per job, fanned\ninto N outputs per the per-mime-group catalog. Mirrors the\n`merge` / `convert` catalog-split-by-mime-group pattern.\n\n**Three shared modes** (audio + video; the mode discriminator\ngates which option applies per the `depends_on` rules in this\nschema):\n- `interval`: split every N numeric-seconds.\n- `count`: split into N equal-duration pieces (integer 2..=200).\n- `cut_points`: explicit cut points in numeric-seconds (strictly\n  increasing, no duplicates, each > 0 and < probed duration).\n\n**Plus an audio-only `silence` mode** (`beta`, gated by the\n`silence_mode_audio` feature): cut at detected silence gaps via\nserver-side silence detection. Video stays three-mode.\n\n**GIF + PDF use range-based selection** instead of three modes:\n- GIF (`image_gif`): `frame_range` REQUIRED + `output_format`\n  enum {png,webp,jpg}.\n- PDF (`document_pdf`): `page_range` OR `page_groups` (mutually\n  exclusive per `depends_on`).\n\n**Wire format**: numeric seconds (floats allowed for sub-second\nprecision). NOT ISO 8601 duration strings. Matches common\nvideo-tooling conventions.\n\n**200-output hard cap** (`OperationResult.outputs[].maxItems:\n200`). Preflight rejects\nrequests exceeding this BEFORE work as `invalid_options` (422).\nCap math by mode:\n- `interval`: `ceil(probed_duration / interval) <= 200`\n- `count`: enforced by the option's `min: 2 max: 200` range\n- `cut_points`: `cut_points.len() + 1 <= 200`\n- `silence` (audio): resolved segment count (detected gaps + 1)\n  `<= 200`; preflight rejects as `invalid_options` (422)\n- PDF: resolved page count after expansion `<= 200`\n  (cross-ref [`WgCqnMRa`](https://trello.com/c/WgCqnMRa))\n- GIF: resolved frame count after expansion `<= 200`\n\n**Output naming**: 3-digit zero-padded \u2014 `output-001` ..\n`output-200`. Matches the convert PDF\u2192N precedent.\n\n**Output envelope binding**:\n- `image_gif`: `PositionIndexed` (frame stream-position 0-based\n  ordinal)\n- `document_pdf`: `PageIndexed` (1-based gapless page index)\n- `audio`: `PositionIndexed` (cut stream-position ordinal)\n- `video`: `PositionIndexed` (cut stream-position ordinal)\n\n**`precision` flag** (audio + video only): `fast` (default) =\npacket-boundary approximate cuts; `exact` = re-encode-aligned\nprecise cuts. Mime_group scoping is the gate \u2014 precision does\nNOT apply to GIF or PDF (frame extraction is exact and PDF page\nselection is exact by definition).\n\n**Long-form video** routes to a separate long-form processing\npath (same `split` OperationType; routing via `processing_class`).\nNo schema-side discriminator beyond MIME detection +\n`processing_class`.\n\n`availability: beta` for the `audio` and `video` mime_groups\n(shape-stable + opt-in, MUST NOT 422). Video activates **both\nclasses**: `video.processing_class.short_form` AND `long_form` are\n`beta`. The `image_gif` and `document_pdf` mime_groups stay\n`availability: planned` until their cross-repo backend support ships;\ndispatch returns `feature_not_available` (422) for the still-planned\ngroups/classes until then.\n",
      "features": {
        "silence_mode_audio": {
          "availability": "beta",
          "description": "Silence-detect cut mode for audio (`mode: silence` \u2014\nserver-side silence detection). `beta`: shape-stable + opt-in.\nThe mode ships dark until the API regenerates enum validation\nto accept `mode: silence`. The `silence` enum value carries a\nmatching `per_value_availability: beta` on `audio.mode`.\n"
        }
      },
      "input_model": "single",
      "mime_groups": {
        "audio": {
          "availability": "beta",
          "mimes": [
            "audio/mpeg",
            "audio/aac",
            "audio/ogg",
            "audio/flac",
            "audio/wav",
            "audio/mp4"
          ],
          "options": {
            "count": {
              "depends_on": {
                "mode": "count"
              },
              "description": "Number of equal-duration output pieces. Range 2..=200\nenforced declaratively (1 = no-op, rejected;\ncap = 200-output ceiling). Only valid when `mode: count`.\n",
              "max": 200,
              "min": 2,
              "required": true,
              "type": "integer"
            },
            "cut_points": {
              "depends_on": {
                "mode": "cut_points"
              },
              "description": "Cut points in numeric seconds (float, sub-second allowed).\nArray entries; NOT a comma-separated string. MUST be\nstrictly increasing (preflight-validated against probe);\neach entry > 0 and < probed duration. `uniqueItems: true`\ncatches accidental duplicates at the OpenAPI layer; strict\nmonotonicity and the < probed-duration bound are\npreflight-checked at workflow-create. Output count =\n`cut_points.length + 1`; `maxItems: 199` expresses the\n200-output cap declaratively. Only valid when\n`mode: cut_points`.\n",
              "items": {
                "exclusiveMinimum": 0,
                "type": "number"
              },
              "maxItems": 199,
              "minItems": 1,
              "required": true,
              "type": "array",
              "uniqueItems": true
            },
            "interval": {
              "depends_on": {
                "mode": "interval"
              },
              "description": "Cut interval in numeric seconds (float, sub-second\nallowed). `ceil(probed_duration / interval) <= 200`\npreflight rule applies. Only valid when `mode: interval`.\n",
              "min": 0.001,
              "required": true,
              "type": "float"
            },
            "mode": {
              "default": "interval",
              "description": "Split strategy:\n- `interval`: cut every `interval` seconds.\n- `count`: cut into `count` equal-duration pieces.\n- `cut_points`: cut at explicit timestamps.\n- `silence`: cut at detected silence gaps (audio-only,\n  `beta` via the `silence_mode_audio` feature gate).\n",
              "per_value_availability": {
                "silence": {
                  "availability": "beta"
                }
              },
              "type": "enum",
              "values": [
                "interval",
                "count",
                "cut_points",
                "silence"
              ]
            },
            "precision": {
              "default": "fast",
              "description": "Cut precision. Under `fast`, audio cuts are\npacket-boundary approximate (drift bounded by codec\npacket size \u2014 typically <= 23ms for AAC, <= 26ms for\nMP3). Under `exact`, the server re-encodes to align cuts\nprecisely at the requested timestamps (slower, larger\noutput, but sample-accurate).\n",
              "type": "enum",
              "values": [
                "fast",
                "exact"
              ]
            },
            "silence_handling": {
              "default": "keep_at_end",
              "depends_on": {
                "mode": "silence"
              },
              "description": "How detected silence is handled at segment boundaries.\n`keep_at_end`: trailing silence stays appended to the\npreceding segment (no separate silence-only outputs).\nOnly valid when `mode: silence`.\n",
              "type": "enum",
              "values": [
                "keep_at_end"
              ]
            },
            "silence_min_duration": {
              "default": 0.75,
              "depends_on": {
                "mode": "silence"
              },
              "description": "Minimum duration of a silence gap, in numeric seconds\n(sub-second allowed), before it is treated as a cut point.\n`min: 0.001` is the strict-positive sub-second idiom\n(cf. `interval`). Only valid when `mode: silence`.\n",
              "min": 0.001,
              "type": "float"
            },
            "silence_threshold_db": {
              "default": -50.0,
              "depends_on": {
                "mode": "silence"
              },
              "description": "Noise floor below which audio counts as silence, in dBFS\n(negative; 0.0 = full scale). Strictly negative in\npractice; `max: 0.0` is the declarative ceiling (a 0 dB\n\"silence\" floor is degenerate, like the strict bounds\ndocumented in prose for `cut_points`). Only valid when\n`mode: silence`.\n",
              "max": 0.0,
              "type": "float"
            }
          }
        },
        "document_pdf": {
          "availability": "planned",
          "mimes": [
            "application/pdf"
          ],
          "options": {
            "mode": {
              "default": "page_range",
              "description": "PDF split strategy:\n- `page_range`: select pages via a range expression.\n- `page_groups`: group N pages per output.\n",
              "type": "enum",
              "values": [
                "page_range",
                "page_groups"
              ]
            },
            "page_groups": {
              "depends_on": {
                "mode": "page_groups"
              },
              "description": "Group N pages per output (e.g. `page_groups: 5` on a\n50-page PDF produces 10 outputs of 5 pages each).\nRequired when `mode: page_groups`. The page-count cap\napplies to the resolved group count: `ceil(total_pages /\npage_groups) <= 200`.\n",
              "max": 200,
              "min": 2,
              "required": true,
              "type": "integer"
            },
            "page_range": {
              "depends_on": {
                "mode": "page_range"
              },
              "description": "Page-range expression (e.g. \"1-5\", \"1,3,5-7\", \"10-20\").\n1-based inclusive. Required when `mode: page_range`.\nResolved page count MUST be <= 200 (`invalid_options`\n422 if exceeded \u2014 cross-ref\n[`WgCqnMRa`](https://trello.com/c/WgCqnMRa)).\n",
              "pattern": "^\\d+(-\\d+)?(,\\d+(-\\d+)?)*$",
              "required": true,
              "type": "string"
            }
          }
        },
        "image_gif": {
          "mimes": [
            "image/gif"
          ],
          "options": {
            "frame_range": {
              "description": "Frame range as `start-end` integer pair (e.g. \"1-100\").\nREQUIRED in V1 \u2014 no silent default. Without explicit\nrange, a 30-second GIF at 24 fps = 720 frames would blow\nthe 200-output cap on every input. Both start and end are\n1-based, inclusive. Resolved frame count MUST be <= 200\n(`invalid_options` 422 if exceeded).\n",
              "pattern": "^\\d+-\\d+$",
              "required": true,
              "type": "string"
            },
            "output_format": {
              "default": "png",
              "description": "Output image format for each extracted frame. Aspect ratio\nand dimensions preserved from the source GIF.\n",
              "type": "enum",
              "values": [
                "png",
                "webp",
                "jpg"
              ]
            }
          }
        },
        "video": {
          "availability": "beta",
          "mimes": [
            "video/mp4",
            "video/webm",
            "video/quicktime"
          ],
          "options": {
            "count": {
              "depends_on": {
                "mode": "count"
              },
              "description": "Number of equal-duration output pieces. Range 2..=200.\nOnly valid when `mode: count`.\n",
              "max": 200,
              "min": 2,
              "required": true,
              "type": "integer"
            },
            "cut_points": {
              "depends_on": {
                "mode": "cut_points"
              },
              "description": "Cut points in numeric seconds (float, sub-second allowed).\nArray entries; NOT a comma-separated string. Strictly\nincreasing (preflight-validated against probe); each entry\n> 0 and < probed duration. `uniqueItems: true` catches\naccidental duplicates at the OpenAPI layer; strict\nmonotonicity and the < probed-duration bound are\npreflight-checked at workflow-create. Output count =\n`cut_points.length + 1`; `maxItems: 199` expresses the\n200-output cap declaratively. Only valid when\n`mode: cut_points`.\n",
              "items": {
                "exclusiveMinimum": 0,
                "type": "number"
              },
              "maxItems": 199,
              "minItems": 1,
              "required": true,
              "type": "array",
              "uniqueItems": true
            },
            "interval": {
              "depends_on": {
                "mode": "interval"
              },
              "description": "Cut interval in numeric seconds. `ceil(probed_duration /\ninterval) <= 200`. Only valid when `mode: interval`.\n",
              "min": 0.001,
              "required": true,
              "type": "float"
            },
            "mode": {
              "default": "interval",
              "description": "Split strategy:\n- `interval`: cut every `interval` seconds.\n- `count`: cut into `count` equal-duration pieces.\n- `cut_points`: cut at explicit timestamps.\n",
              "type": "enum",
              "values": [
                "interval",
                "count",
                "cut_points"
              ]
            },
            "precision": {
              "default": "fast",
              "description": "Cut precision. Under `fast`, video cuts are keyframe-\naligned (drift bounded by GOP size \u2014 typically up to 1-2\nseconds depending on encoder). Under `exact`, the backend\nre-encodes to align cuts precisely at the requested\ntimestamps (slower, larger output, but frame-accurate).\n",
              "type": "enum",
              "values": [
                "fast",
                "exact"
              ]
            }
          },
          "processing_class": {
            "long_form": {
              "availability": "beta",
              "constraints": {
                "max_input_duration": "PT12H",
                "max_input_size_bytes": 5000000000,
                "max_output_size_bytes": 5000000000
              },
              "per_tier_constraints": {
                "enterprise": {
                  "max_input_size_bytes": 120000000000,
                  "max_output_size_bytes": 120000000000
                },
                "max": {
                  "max_input_size_bytes": 50000000000,
                  "max_output_size_bytes": 50000000000
                }
              },
              "required_tier": "pro"
            },
            "short_form": {
              "availability": "beta",
              "constraints": {
                "max_input_duration": "PT5M",
                "max_input_size_bytes": 524288000,
                "max_output_size_bytes": 524288000
              }
            }
          }
        }
      },
      "sole_op": true
    },
    "text_watermark": {
      "default": false,
      "description": "Render a text overlay onto an image using bundled Liberation Sans\n(SIL OFL). Single-input \u2014 text comes from options, not from a file\ninput. Source capped at 100 megapixels in `single` mode. `tiled`\nmode keeps the 25-megapixel canvas bound (see `tile_spacing` /\n`watermark_mode`).\n\nTwo rendering modes:\n- `single`: one label rendered at the anchor + margin position.\n- `tiled`: text tiled across the source image (Cinavia / Adobe Stock\n  pattern); rotation default -45\u00b0 and tile_spacing controls density.\n\nStable today for image/jpeg, image/png, image/webp inputs. TIFF\n(image/tiff) and BMP (image/bmp) inputs are now `stable` too (flipped\nplanned->stable 2026-06-30 on the lambdas staging proof, UANjD38A /\n#296) via the parallel `image_tiff` / `image_bmp` mime_groups. Output\nmirrors the input format (`produces: same_as_input`), so a format is\nonly advertised when the worker can both decode AND encode it;\nHEIC/AVIF (no encoder) and SVG (cannot re-emit svg from a rasterised\ncomposite) stay out of scope.\n",
      "input_model": "single",
      "mime_groups": {
        "image": {
          "mimes": [
            "image/jpeg",
            "image/png",
            "image/webp"
          ],
          "options": {
            "anchor": {
              "default": "bottom_right",
              "description": "9-grid anchor position. For watermark_mode: single, the text\nlabel is placed at this anchor + margin_x/margin_y offset.\nFor watermark_mode: tiled, anchor is ignored (tiles fill the\nsource image).\n",
              "type": "enum",
              "values": [
                "top_left",
                "top_center",
                "top_right",
                "center_left",
                "center",
                "center_right",
                "bottom_left",
                "bottom_center",
                "bottom_right"
              ]
            },
            "color": {
              "default": "#FFFFFF80",
              "description": "Text colour as hex RGB (#RRGGBB) or RGBA (#RRGGBBAA).\nDefault is white with 50% alpha.\n",
              "pattern": "^#[0-9A-Fa-f]{6}([0-9A-Fa-f]{2})?$",
              "type": "string"
            },
            "font_family": {
              "default": "liberation_sans",
              "description": "Font family. V2.0 ships with bundled Liberation Sans only;\nuser-uploaded fonts are a future extension (separate ticket).\n",
              "type": "enum",
              "values": [
                "liberation_sans"
              ]
            },
            "font_size": {
              "default": 48.0,
              "description": "Font size in pixels. Clamped to range.",
              "max": 512.0,
              "min": 8.0,
              "type": "float"
            },
            "margin_x": {
              "default": "0px",
              "description": "Horizontal offset from the anchor as pixels (e.g. \"40px\") or\npercentage of base width (e.g. \"5%\"). Direction is\nanchor-relative. Ignored when watermark_mode: tiled.\n\n**`anchor: center` exception** (watermark_mode: single only).\nWhen the anchor is `center` the centre is the centre \u2014 there\nis no anchor-relative direction. Non-zero `margin_x`\n(anything other than the default `\"0px\"` or the equivalent\n`\"0%\"`) combined with `anchor: center` is rejected by the\nAPI at workflow-create time as `invalid_options` (HTTP 422).\nTo nudge a roughly-centred label, pick a peripheral anchor\n(e.g. `top_left`) and use `margin_x` / `margin_y` to\nposition it. Not applicable to watermark_mode: tiled, where\nanchor + margin are ignored entirely.\n",
              "pattern": "^\\d+(\\.\\d+)?(px|%)$",
              "type": "string"
            },
            "margin_y": {
              "default": "0px",
              "description": "Vertical offset from the anchor as pixels or percentage of\nbase height. Ignored when watermark_mode: tiled.\n\n**`anchor: center` exception** (watermark_mode: single only).\nSame rejection rule as `margin_x` \u2014 non-zero `margin_y` with\n`anchor: center` is rejected by the API at workflow-create\ntime as `invalid_options` (HTTP 422).\n",
              "pattern": "^\\d+(\\.\\d+)?(px|%)$",
              "type": "string"
            },
            "opacity": {
              "default": 1.0,
              "description": "Text overlay opacity. Default 1.0 \u2014 the alpha channel of\n`color` already encodes transparency, so opacity is usually\nleft at 1.0 and transparency is set via the color hex.\n",
              "max": 1.0,
              "min": 0.0,
              "type": "float"
            },
            "rotation": {
              "default": -45.0,
              "description": "Rotation angle in degrees. Default -45\u00b0 suits tiled mode\n(angled diagonal pattern). Clamped to range.\n",
              "max": 360.0,
              "min": -360.0,
              "type": "float"
            },
            "text": {
              "description": "Watermark text. Must be non-empty.\n",
              "required": true,
              "type": "string"
            },
            "tile_spacing": {
              "depends_on": {
                "watermark_mode": "tiled"
              },
              "description": "Spacing between tiled labels in pixels. Defaults to font_size\nwhen omitted. Only applies when watermark_mode is tiled.\n",
              "max": 1000,
              "min": 0,
              "type": "integer"
            },
            "watermark_mode": {
              "default": "single",
              "description": "Rendering mode:\n- `single`: one label placed at the anchor + margin offset.\n- `tiled`: repeated tiles across the entire source image at\n  the rotation angle.\n",
              "type": "enum",
              "values": [
                "single",
                "tiled"
              ]
            }
          }
        },
        "image_bmp": {
          "mimes": [
            "image/bmp"
          ],
          "options": {
            "anchor": {
              "default": "bottom_right",
              "description": "9-grid anchor position. For watermark_mode: single, the text\nlabel is placed at this anchor + margin_x/margin_y offset.\nFor watermark_mode: tiled, anchor is ignored (tiles fill the\nsource image).\n",
              "type": "enum",
              "values": [
                "top_left",
                "top_center",
                "top_right",
                "center_left",
                "center",
                "center_right",
                "bottom_left",
                "bottom_center",
                "bottom_right"
              ]
            },
            "color": {
              "default": "#FFFFFF80",
              "description": "Text colour as hex RGB (#RRGGBB) or RGBA (#RRGGBBAA).\nDefault is white with 50% alpha.\n",
              "pattern": "^#[0-9A-Fa-f]{6}([0-9A-Fa-f]{2})?$",
              "type": "string"
            },
            "font_family": {
              "default": "liberation_sans",
              "description": "Font family. V2.0 ships with bundled Liberation Sans only;\nuser-uploaded fonts are a future extension (separate ticket).\n",
              "type": "enum",
              "values": [
                "liberation_sans"
              ]
            },
            "font_size": {
              "default": 48.0,
              "description": "Font size in pixels. Clamped to range.",
              "max": 512.0,
              "min": 8.0,
              "type": "float"
            },
            "margin_x": {
              "default": "0px",
              "description": "Horizontal offset from the anchor as pixels (e.g. \"40px\") or\npercentage of base width (e.g. \"5%\"). Direction is\nanchor-relative. Ignored when watermark_mode: tiled.\n\n**`anchor: center` exception** (watermark_mode: single only).\nNon-zero `margin_x` combined with `anchor: center` is rejected\nby the API at workflow-create time as `invalid_options`\n(HTTP 422). Not applicable to watermark_mode: tiled, where\nanchor + margin are ignored entirely.\n",
              "pattern": "^\\d+(\\.\\d+)?(px|%)$",
              "type": "string"
            },
            "margin_y": {
              "default": "0px",
              "description": "Vertical offset from the anchor as pixels or percentage of\nbase height. Ignored when watermark_mode: tiled.\n\n**`anchor: center` exception** (watermark_mode: single only).\nSame rejection rule as `margin_x` \u2014 non-zero `margin_y` with\n`anchor: center` is rejected by the API at workflow-create\ntime as `invalid_options` (HTTP 422).\n",
              "pattern": "^\\d+(\\.\\d+)?(px|%)$",
              "type": "string"
            },
            "opacity": {
              "default": 1.0,
              "description": "Text overlay opacity. Default 1.0 \u2014 the alpha channel of\n`color` already encodes transparency, so opacity is usually\nleft at 1.0 and transparency is set via the color hex.\n",
              "max": 1.0,
              "min": 0.0,
              "type": "float"
            },
            "rotation": {
              "default": -45.0,
              "description": "Rotation angle in degrees. Default -45\u00b0 suits tiled mode\n(angled diagonal pattern). Clamped to range.\n",
              "max": 360.0,
              "min": -360.0,
              "type": "float"
            },
            "text": {
              "description": "Watermark text. Must be non-empty.\n",
              "required": true,
              "type": "string"
            },
            "tile_spacing": {
              "depends_on": {
                "watermark_mode": "tiled"
              },
              "description": "Spacing between tiled labels in pixels. Defaults to font_size\nwhen omitted. Only applies when watermark_mode is tiled.\n",
              "max": 1000,
              "min": 0,
              "type": "integer"
            },
            "watermark_mode": {
              "default": "single",
              "description": "Rendering mode:\n- `single`: one label placed at the anchor + margin offset.\n- `tiled`: repeated tiles across the entire source image at\n  the rotation angle.\n",
              "type": "enum",
              "values": [
                "single",
                "tiled"
              ]
            }
          }
        },
        "image_tiff": {
          "mimes": [
            "image/tiff"
          ],
          "options": {
            "anchor": {
              "default": "bottom_right",
              "description": "9-grid anchor position. For watermark_mode: single, the text\nlabel is placed at this anchor + margin_x/margin_y offset.\nFor watermark_mode: tiled, anchor is ignored (tiles fill the\nsource image).\n",
              "type": "enum",
              "values": [
                "top_left",
                "top_center",
                "top_right",
                "center_left",
                "center",
                "center_right",
                "bottom_left",
                "bottom_center",
                "bottom_right"
              ]
            },
            "color": {
              "default": "#FFFFFF80",
              "description": "Text colour as hex RGB (#RRGGBB) or RGBA (#RRGGBBAA).\nDefault is white with 50% alpha.\n",
              "pattern": "^#[0-9A-Fa-f]{6}([0-9A-Fa-f]{2})?$",
              "type": "string"
            },
            "font_family": {
              "default": "liberation_sans",
              "description": "Font family. V2.0 ships with bundled Liberation Sans only;\nuser-uploaded fonts are a future extension (separate ticket).\n",
              "type": "enum",
              "values": [
                "liberation_sans"
              ]
            },
            "font_size": {
              "default": 48.0,
              "description": "Font size in pixels. Clamped to range.",
              "max": 512.0,
              "min": 8.0,
              "type": "float"
            },
            "margin_x": {
              "default": "0px",
              "description": "Horizontal offset from the anchor as pixels (e.g. \"40px\") or\npercentage of base width (e.g. \"5%\"). Direction is\nanchor-relative. Ignored when watermark_mode: tiled.\n\n**`anchor: center` exception** (watermark_mode: single only).\nNon-zero `margin_x` combined with `anchor: center` is rejected\nby the API at workflow-create time as `invalid_options`\n(HTTP 422). Not applicable to watermark_mode: tiled, where\nanchor + margin are ignored entirely.\n",
              "pattern": "^\\d+(\\.\\d+)?(px|%)$",
              "type": "string"
            },
            "margin_y": {
              "default": "0px",
              "description": "Vertical offset from the anchor as pixels or percentage of\nbase height. Ignored when watermark_mode: tiled.\n\n**`anchor: center` exception** (watermark_mode: single only).\nSame rejection rule as `margin_x` \u2014 non-zero `margin_y` with\n`anchor: center` is rejected by the API at workflow-create\ntime as `invalid_options` (HTTP 422).\n",
              "pattern": "^\\d+(\\.\\d+)?(px|%)$",
              "type": "string"
            },
            "opacity": {
              "default": 1.0,
              "description": "Text overlay opacity. Default 1.0 \u2014 the alpha channel of\n`color` already encodes transparency, so opacity is usually\nleft at 1.0 and transparency is set via the color hex.\n",
              "max": 1.0,
              "min": 0.0,
              "type": "float"
            },
            "rotation": {
              "default": -45.0,
              "description": "Rotation angle in degrees. Default -45\u00b0 suits tiled mode\n(angled diagonal pattern). Clamped to range.\n",
              "max": 360.0,
              "min": -360.0,
              "type": "float"
            },
            "text": {
              "description": "Watermark text. Must be non-empty.\n",
              "required": true,
              "type": "string"
            },
            "tile_spacing": {
              "depends_on": {
                "watermark_mode": "tiled"
              },
              "description": "Spacing between tiled labels in pixels. Defaults to font_size\nwhen omitted. Only applies when watermark_mode is tiled.\n",
              "max": 1000,
              "min": 0,
              "type": "integer"
            },
            "watermark_mode": {
              "default": "single",
              "description": "Rendering mode:\n- `single`: one label placed at the anchor + margin offset.\n- `tiled`: repeated tiles across the entire source image at\n  the rotation angle.\n",
              "type": "enum",
              "values": [
                "single",
                "tiled"
              ]
            }
          }
        }
      },
      "sole_op": true
    },
    "thumbnail": {
      "default": false,
      "description": "Resize an image to specific dimensions, or extract a preview frame/page from a video or document. For images this is a full resizer (1-16384px), not just small previews.",
      "input_model": "single",
      "mime_groups": {
        "document": {
          "max_output_pixels": 16000000,
          "mimes": [
            "application/pdf",
            "application/epub+zip",
            "application/vnd.openxmlformats-officedocument.wordprocessingml.document",
            "application/vnd.openxmlformats-officedocument.spreadsheetml.sheet",
            "application/vnd.openxmlformats-officedocument.presentationml.presentation",
            "application/vnd.oasis.opendocument.text",
            "application/vnd.oasis.opendocument.spreadsheet",
            "application/vnd.oasis.opendocument.presentation"
          ],
          "options": {
            "fit": {
              "default": "crop",
              "description": "Resize mode",
              "type": "enum",
              "values": [
                "max",
                "crop",
                "scale"
              ]
            },
            "format": {
              "default": "jpg",
              "description": "Output format for the thumbnail",
              "type": "enum",
              "values": [
                "jpg",
                "png",
                "webp"
              ]
            },
            "height": {
              "description": "Target height in pixels (1-16384; width*height <= 16MP). Optional \u2014 see `width` for the omitted-default behaviour.",
              "max": 16384,
              "min": 1,
              "type": "integer"
            },
            "page": {
              "default": 1,
              "depends_on": {
                "source": "page"
              },
              "description": "1-based index of the printed page to render, after print-layout pagination is applied. For office formats this is the page in the document's print layout \u2014 NOT a 1:1 sheet/slide index: a spreadsheet sheet may span multiple printed pages and multi-sheet workbooks paginate their sheets in order. For PDF inputs it is the PDF page number directly.",
              "min": 1,
              "type": "integer"
            },
            "quality": {
              "default": 85,
              "depends_on": {
                "format": [
                  "jpg",
                  "webp"
                ]
              },
              "description": "Output quality for lossy thumbnail formats (jpg/webp): higher = better quality, larger file. Ignored for png (lossless).",
              "max": 100,
              "min": 1,
              "type": "integer"
            },
            "source": {
              "default": "page",
              "description": "page = render a specific page/slide, cover = extract cover image (EPUB only, ignored for other formats)",
              "type": "enum",
              "values": [
                "page",
                "cover"
              ]
            },
            "width": {
              "description": "Target width in pixels (1-16384; width*height <= 16MP). Optional \u2014 when omitted the server applies an aspect-preserving default (both omitted \u2192 longest edge 320px; only one of width/height provided \u2192 the other is derived from the source aspect ratio).",
              "max": 16384,
              "min": 1,
              "type": "integer"
            }
          }
        },
        "image": {
          "max_output_pixels": 16000000,
          "mimes": [
            "image/jpeg",
            "image/png",
            "image/gif",
            "image/webp",
            "image/tiff",
            "image/bmp",
            "image/avif",
            "image/heic"
          ],
          "options": {
            "background": {
              "depends_on": {
                "format": "jpg"
              },
              "description": "Background colour as a 6-digit hex (`#RRGGBB`, e.g. `#ffffff`) for a transparent image (e.g. PNG/WebP with alpha) resized to an opaque JPG thumbnail \u2014 the alpha-flatten fill colour. Mirrors convert.image.background. HEX ONLY: a non-hex value (e.g. a CSS named colour) is rejected rather than advertising an inert free-form string. When omitted, transparency is flattened onto white. JPG output only: PNG/WebP keep alpha, so the flatten (and this option) do not apply.",
              "pattern": "^#[0-9a-fA-F]{6}$",
              "type": "string"
            },
            "fit": {
              "default": "crop",
              "description": "Resize mode",
              "type": "enum",
              "values": [
                "max",
                "crop",
                "scale"
              ]
            },
            "format": {
              "default": "jpg",
              "description": "Output format for the thumbnail",
              "type": "enum",
              "values": [
                "jpg",
                "png",
                "webp"
              ]
            },
            "height": {
              "description": "Target height in pixels (1-16384; width*height <= 16MP). For image input this is a full resize. Optional \u2014 see `width` for the omitted-default (aspect-preserving) behaviour.",
              "max": 16384,
              "min": 1,
              "type": "integer"
            },
            "quality": {
              "default": 85,
              "depends_on": {
                "format": [
                  "jpg",
                  "webp",
                  "png"
                ]
              },
              "description": "Output quality for lossy thumbnail formats (jpg / webp / png): higher = better quality, larger file. For PNG, `quality` < 100 applies lossy palette quantization (imagequant \u2014 transparency preserved); `quality: 100` keeps the PNG lossless. Per-format effective default (server-resolved when `quality` is unset, per ADR-0020 D1): PNG \u2192 70 (more aggressive \u2014 thumbnails target small files), jpg/webp \u2192 85. The schema `default` is the nominal jpg/webp value (85); the FE shows 70 as the initial PNG slider value.",
              "max": 100,
              "min": 1,
              "type": "integer"
            },
            "width": {
              "description": "Target width in pixels (1-16384; width*height <= 16MP). For image input this is a full resize, not just a small preview. Optional \u2014 when omitted the server applies an aspect-preserving default (both omitted \u2192 longest edge 320px; only one of width/height provided \u2192 the other is derived from the source aspect ratio), so non-square images are never distorted.",
              "max": 16384,
              "min": 1,
              "type": "integer"
            }
          }
        },
        "video": {
          "max_output_pixels": 16000000,
          "mimes": [
            "video/mp4",
            "video/webm",
            "video/quicktime"
          ],
          "options": {
            "fit": {
              "default": "crop",
              "description": "Resize mode",
              "type": "enum",
              "values": [
                "max",
                "crop",
                "scale"
              ]
            },
            "format": {
              "default": "jpg",
              "description": "Output format for the thumbnail",
              "type": "enum",
              "values": [
                "jpg",
                "png",
                "webp"
              ]
            },
            "height": {
              "description": "Target height in pixels (1-16384; width*height <= 16MP). Rounded down to an even number. Optional \u2014 see `width` for the omitted-default behaviour.",
              "max": 16384,
              "min": 1,
              "type": "integer"
            },
            "quality": {
              "default": 85,
              "depends_on": {
                "format": [
                  "jpg",
                  "webp"
                ]
              },
              "description": "Output quality for lossy thumbnail formats (jpg/webp): higher = better quality, larger file. Ignored for png (lossless).",
              "max": 100,
              "min": 1,
              "type": "integer"
            },
            "timestamp": {
              "default": "00:00:01",
              "description": "Frame to extract (HH:MM:SS or seconds)",
              "type": "string"
            },
            "width": {
              "description": "Target width in pixels (1-16384; width*height <= 16MP). Rounded down to an even number. Optional \u2014 when omitted the server applies an aspect-preserving default (both omitted \u2192 longest edge 320px; only one of width/height provided \u2192 the other is derived from the source aspect ratio).",
              "max": 16384,
              "min": 1,
              "type": "integer"
            }
          },
          "processing_class": {
            "long_form": {
              "availability": "planned",
              "constraints": {
                "max_input_duration": "PT12H",
                "max_input_size_bytes": 5000000000
              },
              "per_tier_constraints": {
                "enterprise": {
                  "max_input_size_bytes": 120000000000
                },
                "max": {
                  "max_input_size_bytes": 50000000000
                }
              },
              "required_tier": "pro"
            },
            "short_form": {
              "availability": "stable",
              "constraints": {
                "max_input_duration": "PT5M",
                "max_input_size_bytes": 524288000
              }
            }
          }
        }
      }
    },
    "transform": {
      "availability": "planned",
      "default": false,
      "description": "Apply a geometric / orientation transform to a media asset. **v1 supports\nrotate + flip only** (orientation transforms) \u2014 it does NOT do crop, scale,\nskew, or colour transforms. The name is deliberately `transform` (not\n`orient`) to leave room for a future `crop` on the same op; do not read the\nname as advertising arbitrary affine transforms. Full design + normative\norder-of-operations, routing topology, chain order, media gating, and\nbilling model: ADR-0026.\n\n**Params (per media group):**\n- `rotate` \u2014 `0 | 90 | 180 | 270` degrees clockwise (default `0`).\n- `flip` \u2014 `none | horizontal | vertical | both` mirror (default `none`).\n  `horizontal` = left\u2194right (the selfie/webcam mirror-correction),\n  `vertical` = top\u2194bottom, `both` = both axes.\n\n**Normative order-of-operations (v1 has NO arbitrary sequence).** For a\nsingle `transform` op the worker applies, in this fixed order:\n1. (images) normalize display orientation from EXIF,\n2. apply `rotate`,\n3. apply `flip` in the FINAL (post-rotate) display axes,\n4. (images) write pixels with the orientation tag reset/removed.\nSo `flip: horizontal` always means left\u2194right in the *visible result*, even\nafter a 90/270 rotate. See ADR-0026 \u00a7Order-of-operations.\n\n**auto_orient coexistence.** `transform` reads the image honouring EXIF\norientation, applies rotate/flip, and emits normalized-orientation pixels \u2014\nso a downstream `compress`/`convert` `auto_orient` is a no-op. Raw\nstored-pixel rotation that ignores EXIF is out of scope for v1.\n\n**No-op is rejected at create.** A `transform` with `rotate: 0` AND\n`flip: none` (for documents: `rotate: 0`) does nothing, so the API rejects\nit as `invalid_options` at create \u2014 there is no billed, worker-dispatched\nno-op (mirrors the API facade gate that rejects inert option combos). At\nleast one of `rotate`/`flip` must be non-default.\n\n**Media scope (v1).** IMAGE **still-raster** (jpeg/png/webp) and animated\nGIF; VIDEO (mp4/webm); DOCUMENT is PDF-rotate ONLY (no flip \u2014 the\n`document_pdf` group offers `rotate` alone). AUDIO is intentionally absent\n(no spatial geometry). SVG, HEIC/AVIF, and non-PDF documents\n(Office/ODF/EPUB) are out of scope for v1 and are not advertised.\n**Animated PNG (APNG) and animated WebP** are NOT covered by the\nstill-raster `image` group's activation proof \u2014 like animated GIF, a\npixel-baking geometry op would drop frames / animation metadata. Before the\n`image` group flips stable the worker MUST either reject animated PNG/WebP\nvariants at probe or transform ALL frames + preserve animation metadata\n(same bar as the `image_gif` group). Everything ships `availability:\nplanned` first and flips per media on worker capability-source proof\n(ADR-0006) \u2014 each activation co-updates schema availability + AsyncAPI\nrouting + API OperationType/chain/pricing + the billing weight row together\n(ADR-0026 \u00a7Rollout).\n\n**Routing (thumbnail precedent).** The payload `operation_type` stays the\nbase value `transform`; the API publisher resolves the effective input MIME\nto an SNS `operation_type` routing-attribute sub-type\n(`transform_image` / `transform_video` / `transform_document`). Those\nsub-types are ROUTING ATTRIBUTES ONLY \u2014 never persisted or public operation\ntypes (they are absent from the OperationType payload enum). The\n`document_pdf` group routes under `transform_document` (media-family level,\neven though v1 is PDF-only). See ADR-0026 \u00a7Routing + asyncapi/events.yaml.\n",
      "input_model": "single",
      "mime_groups": {
        "document_pdf": {
          "availability": "planned",
          "mimes": [
            "application/pdf"
          ],
          "options": {
            "rotate": {
              "default": 0,
              "description": "Clockwise page rotation in degrees, applied to the rendered page orientation. `flip` is NOT offered for documents in v1.",
              "type": "enum",
              "value_type": "integer",
              "values": [
                0,
                90,
                180,
                270
              ]
            }
          }
        },
        "image": {
          "availability": "planned",
          "mimes": [
            "image/jpeg",
            "image/png",
            "image/webp"
          ],
          "options": {
            "flip": {
              "default": "none",
              "description": "Mirror the image. `horizontal` = left\u2194right, `vertical` = top\u2194bottom, `both` = both axes. Applied AFTER `rotate`, in the final (post-rotate) display axes, so `horizontal` always means left\u2194right in the visible result.",
              "type": "enum",
              "values": [
                "none",
                "horizontal",
                "vertical",
                "both"
              ]
            },
            "rotate": {
              "default": 0,
              "description": "Clockwise rotation in degrees. Applied BEFORE `flip` (see the operation-level normative order-of-operations).",
              "type": "enum",
              "value_type": "integer",
              "values": [
                0,
                90,
                180,
                270
              ]
            }
          }
        },
        "image_gif": {
          "availability": "planned",
          "mimes": [
            "image/gif"
          ],
          "options": {
            "flip": {
              "default": "none",
              "description": "Mirror all frames. Applied after `rotate`, in final display axes.",
              "type": "enum",
              "values": [
                "none",
                "horizontal",
                "vertical",
                "both"
              ]
            },
            "rotate": {
              "default": 0,
              "description": "Clockwise rotation in degrees (all frames). Applied before `flip`.",
              "type": "enum",
              "value_type": "integer",
              "values": [
                0,
                90,
                180,
                270
              ]
            }
          }
        },
        "video": {
          "mimes": [
            "video/mp4",
            "video/webm"
          ],
          "options": {
            "flip": {
              "default": "none",
              "description": "Mirror the video (FFmpeg `hflip` / `vflip`). `horizontal` = left\u2194right, `vertical` = top\u2194bottom, `both` = both. Applied AFTER `rotate`, in the final display axes. Re-encodes the video.",
              "type": "enum",
              "values": [
                "none",
                "horizontal",
                "vertical",
                "both"
              ]
            },
            "rotate": {
              "default": 0,
              "description": "Clockwise rotation in degrees (FFmpeg `transpose`). Applied BEFORE `flip`. Re-encodes the video.",
              "type": "enum",
              "value_type": "integer",
              "values": [
                0,
                90,
                180,
                270
              ]
            }
          },
          "processing_class": {
            "long_form": {
              "availability": "planned",
              "constraints": {
                "max_input_duration": "PT12H",
                "max_input_size_bytes": 5000000000,
                "max_output_size_bytes": 5000000000
              },
              "per_tier_constraints": {
                "enterprise": {
                  "max_input_size_bytes": 120000000000,
                  "max_output_size_bytes": 120000000000
                },
                "max": {
                  "max_input_size_bytes": 50000000000,
                  "max_output_size_bytes": 50000000000
                }
              },
              "required_tier": "pro"
            },
            "short_form": {
              "availability": "planned",
              "constraints": {
                "max_input_duration": "PT5M",
                "max_input_size_bytes": 524288000,
                "max_output_size_bytes": 524288000
              }
            }
          }
        }
      },
      "sole_op": false
    },
    "video_text_watermark": {
      "availability": "planned",
      "default": false,
      "description": "Render a text overlay onto a base video using bundled Liberation\nSans (SIL OFL). Single-input \u2014 the text and its styling come from\noptions, not from a file input.\n\n**Dedicated operation, not an extension of `text_watermark`.**\n`text_watermark` renders onto images (single-pass); per-frame video\ntext rendering requires a re-encode (overlay = pixel modification,\nno stream-copy escape). Different runtime.\n\n**Audio passthrough.** The base video's audio stream is preserved\nunchanged (stream-copied) \u2014 the watermark applies to the visual\ntrack only.\n\nTwo rendering modes (mirrors `text_watermark`):\n- `single`: one label rendered at the anchor + margin position.\n- `tiled`: text tiled across the source frames at the rotation\n  angle.\n\n`availability: planned` until the cross-repo backend support ships;\ndispatch returns `feature_not_available` (422) until then.\n",
      "input_model": "single",
      "mime_groups": {
        "video": {
          "mimes": [
            "video/mp4",
            "video/webm"
          ],
          "options": {
            "anchor": {
              "default": "bottom_right",
              "description": "9-grid anchor position. For `watermark_mode: single`, the\ntext label is placed at this anchor + margin_x/margin_y\noffset on each frame. For `watermark_mode: tiled`, anchor\nis ignored (tiles fill the frame).\n",
              "type": "enum",
              "values": [
                "top_left",
                "top_center",
                "top_right",
                "center_left",
                "center",
                "center_right",
                "bottom_left",
                "bottom_center",
                "bottom_right"
              ]
            },
            "color": {
              "default": "#FFFFFF80",
              "description": "Text colour as hex RGB (#RRGGBB) or RGBA (#RRGGBBAA).\nDefault is white with 50% alpha. Same vocabulary as\n`text_watermark.image.color`.\n",
              "pattern": "^#[0-9A-Fa-f]{6}([0-9A-Fa-f]{2})?$",
              "type": "string"
            },
            "font_family": {
              "default": "liberation_sans",
              "description": "Font family. V2.0 ships with bundled Liberation Sans only;\nuser-uploaded fonts are a future extension (separate\nticket).\n",
              "type": "enum",
              "values": [
                "liberation_sans"
              ]
            },
            "font_size": {
              "default": 48.0,
              "description": "Font size in pixels. Clamped to range.",
              "max": 512.0,
              "min": 8.0,
              "type": "float"
            },
            "margin_x": {
              "default": "0px",
              "description": "Horizontal offset from the anchor as pixels (e.g. \"40px\")\nor percentage of video frame width (e.g. \"5%\").\nAnchor-relative. Ignored when `watermark_mode: tiled`.\n\n**`anchor: center` exception** (watermark_mode: single\nonly). Non-zero `margin_x` combined with `anchor: center`\nis rejected by the API at workflow-create time as\n`invalid_options` (HTTP 422).\n",
              "pattern": "^\\d+(\\.\\d+)?(px|%)$",
              "type": "string"
            },
            "margin_y": {
              "default": "0px",
              "description": "Vertical offset from the anchor as pixels or percentage of\nvideo frame height. Ignored when `watermark_mode: tiled`.\n\n**`anchor: center` exception** (watermark_mode: single\nonly). Same rejection rule as `margin_x`.\n",
              "pattern": "^\\d+(\\.\\d+)?(px|%)$",
              "type": "string"
            },
            "opacity": {
              "default": 1.0,
              "description": "Text overlay opacity. Default 1.0 \u2014 the alpha channel of\n`color` already encodes transparency, so opacity is\nusually left at 1.0 and transparency is set via the color\nhex.\n",
              "max": 1.0,
              "min": 0.0,
              "type": "float"
            },
            "rotation": {
              "default": -45.0,
              "description": "Rotation angle in degrees. Default -45\u00b0 suits tiled mode\n(angled diagonal pattern). Clamped to range.\n",
              "max": 360.0,
              "min": -360.0,
              "type": "float"
            },
            "text": {
              "description": "Watermark text. Must be non-empty.\n",
              "required": true,
              "type": "string"
            },
            "tile_spacing": {
              "depends_on": {
                "watermark_mode": "tiled"
              },
              "description": "Spacing between tiled labels in pixels. Defaults to\nfont_size when omitted. Only applies when watermark_mode\nis tiled.\n",
              "max": 1000,
              "min": 0,
              "type": "integer"
            },
            "watermark_mode": {
              "default": "single",
              "description": "Rendering mode:\n- `single`: one label placed at the anchor + margin offset\n  on each frame.\n- `tiled`: repeated tiles across the entire frame at the\n  rotation angle.\n",
              "type": "enum",
              "values": [
                "single",
                "tiled"
              ]
            }
          },
          "processing_class": {
            "long_form": {
              "availability": "planned",
              "constraints": {
                "max_input_duration": "PT12H",
                "max_input_size_bytes": 5000000000,
                "max_output_size_bytes": 5000000000
              },
              "per_tier_constraints": {
                "enterprise": {
                  "max_input_size_bytes": 120000000000,
                  "max_output_size_bytes": 120000000000
                },
                "max": {
                  "max_input_size_bytes": 50000000000,
                  "max_output_size_bytes": 50000000000
                }
              },
              "required_tier": "pro"
            },
            "short_form": {
              "availability": "planned",
              "constraints": {
                "max_input_duration": "PT5M",
                "max_input_size_bytes": 524288000,
                "max_output_size_bytes": 524288000
              }
            }
          }
        }
      },
      "sole_op": true
    },
    "video_watermark": {
      "availability": "beta",
      "default": false,
      "description": "Apply an image overlay onto a base video\nasset. Multi-input role-based:\nexactly one input with `role: base` (the source video) + exactly\none with `role: overlay` (the watermark image). Each input is a\na multi-input source \u2014 an external_import handle, a vault connection,\nor an upstream `job_output` (uploads are NOT referenced directly\ninside inputs[]). To use an uploaded base or overlay, feed it through\na `passthrough` source job and reference it via `{ type: job_output,\nfrom: <id> }` (per ticket 4som89Uh).\n\n**Dedicated operation, not an extension of `image_watermark`.**\n`image_watermark` handles image-on-image overlay only.\n`video_watermark` requires an overlay + re-encode (no stream-copy escape \u2014 overlay = pixel modification).\n\n**Audio passthrough.** The base video's audio stream is preserved\nunchanged (stream-copied) \u2014 the watermark applies to the visual\ntrack only.\n\nActivated to `availability: beta` (Wave A, ticket\n[`c3uthIP4`](https://trello.com/c/c3uthIP4)): the cross-repo backend\nis live (API accepts + publishes via the operations SNS\ntopic), so the operation and its `short_form` processing class are\nshape-stable + opt-in and MUST NOT return `feature_not_available`.\n`long_form` stays `planned` (its long-form path is not yet\nprovisioned) and the `multi_overlay_stack` feature stays `planned`\n(backend support unconfirmed); requesting those planned sub-features\nDOES still return `feature_not_available` until they are separately\nactivated.\n",
      "features": {
        "multi_overlay_stack": {
          "availability": "planned",
          "description": "Allow up to 8 overlay inputs per job (currently capped at 1\noverlay). Per-overlay placement via per-input placement options.\nBackend support not yet confirmed; tagged planned. Mirrors\nthe same-named feature on `image_watermark`.\n"
        }
      },
      "input_model": "multi",
      "max_inputs": 2,
      "mime_groups": {
        "video": {
          "mimes": [
            "video/mp4",
            "video/webm"
          ],
          "options": {
            "anchor": {
              "default": "bottom_right",
              "description": "9-grid anchor position on each video frame. Same vocabulary\nas `image_watermark.image` and `text_watermark.image`.\n",
              "type": "enum",
              "values": [
                "top_left",
                "top_center",
                "top_right",
                "center_left",
                "center",
                "center_right",
                "bottom_left",
                "bottom_center",
                "bottom_right"
              ]
            },
            "margin_x": {
              "default": "0px",
              "description": "Horizontal offset from the anchor as pixels (e.g. \"40px\")\nor percentage of video frame width (e.g. \"5%\"). Direction\nis anchor-relative \u2014 positive moves toward the frame\ncentre.\n\n**`anchor: center` exception.** When the anchor is `center`\nthe centre is the centre \u2014 there is no anchor-relative\ndirection. Non-zero `margin_x` combined with\n`anchor: center` is rejected by the API at workflow-create\ntime as `invalid_options` (HTTP 422). Same rule as the\nother watermark operations.\n",
              "pattern": "^\\d+(\\.\\d+)?(px|%)$",
              "type": "string"
            },
            "margin_y": {
              "default": "0px",
              "description": "Vertical offset from the anchor as pixels or percentage of\nvideo frame height. Anchor-relative; positive moves toward\nthe frame centre.\n\n**`anchor: center` exception.** Same rejection rule as\n`margin_x`.\n",
              "pattern": "^\\d+(\\.\\d+)?(px|%)$",
              "type": "string"
            },
            "opacity": {
              "default": 0.5,
              "description": "Overlay opacity (0 = fully transparent, 1 = fully opaque).\nClamped to range.\n",
              "max": 1.0,
              "min": 0.0,
              "type": "float"
            },
            "overlay_width": {
              "description": "Optional overlay width as pixels or percentage of video\nframe width. Aspect ratio preserved. Omit to use the\noriginal overlay size.\n",
              "pattern": "^\\d+(\\.\\d+)?(px|%)$",
              "type": "string"
            }
          },
          "processing_class": {
            "long_form": {
              "availability": "planned",
              "constraints": {
                "max_input_duration": "PT12H",
                "max_input_size_bytes": 5000000000,
                "max_output_size_bytes": 5000000000
              },
              "per_tier_constraints": {
                "enterprise": {
                  "max_input_size_bytes": 120000000000,
                  "max_output_size_bytes": 120000000000
                },
                "max": {
                  "max_input_size_bytes": 50000000000,
                  "max_output_size_bytes": 50000000000
                }
              },
              "required_tier": "pro"
            },
            "short_form": {
              "availability": "beta",
              "constraints": {
                "max_input_duration": "PT5M",
                "max_input_size_bytes": 524288000,
                "max_output_size_bytes": 524288000
              }
            }
          }
        }
      },
      "min_inputs": 2,
      "per_role_cardinality": {
        "base": {
          "max": 1,
          "min": 1
        },
        "overlay": {
          "max": 1,
          "min": 1
        }
      },
      "sole_op": true
    }
  },
  "schema_version": "2.154.0",
  "source_commit": null,
  "user_tier": null,
  "workflow_features": {
    "delivery": {
      "mode": {
        "per_value_availability": {
          "both": {
            "availability": "planned"
          },
          "bundle": {
            "availability": "planned"
          },
          "individual": {
            "availability": "stable"
          }
        }
      },
      "selection": {
        "type": {
          "per_value_availability": {
            "all_outputs": {
              "availability": "stable"
            },
            "explicit": {
              "availability": "stable"
            },
            "terminal": {
              "availability": "stable"
            }
          }
        }
      }
    }
  }
}
